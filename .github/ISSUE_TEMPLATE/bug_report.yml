name: 问题反馈
description: 安装失败、页面报错、功能不正常——都填这个
title: "[问题] "
labels: ["bug", "待确认"]
body:
  - type: markdown
    attributes:
      value: |
        谢谢你花时间反馈问题！填得越具体，修得越快。
        不会填？遇到安装/使用上的疑问，欢迎去 [讨论区](https://github.com/AxelBeary/Inkglean/discussions) 提问，那边怎么问都行。
  - type: textarea
    id: what-happened
    attributes:
      label: 发生了什么？
      description: 你本来想做什么、实际发生了什么。有报错文字请完整粘贴（黑窗口里的英文也照抄，别只截图一半）。
      placeholder: 我双击了 install.bat，滚了一堆字之后显示……
    validations:
      required: true
  - type: textarea
    id: steps
    attributes:
      label: 你点了什么 / 做了什么
      description: 从哪一步开始，按顺序写。写大白话就行，比如「双击 install.bat → 滚字 → 卡住」。
      placeholder: |
        1. 解压安装包到桌面
        2. 双击 install.bat
        3. 滚了一堆字，最后显示……
    validations:
      required: true
  - type: dropdown
    id: version
    attributes:
      label: 你用的是哪个版本？
      description: 网页端/自建后端的发布版本号；不确定就选「不确定」，别猜。
      options:
        - v1.0.1
        - v1.0.0
        - v1.0.0-beta.4
        - v1.0.0-beta.3
        - v1.0.0-beta.2
        - v1.0.0-beta.1
        - v0.6.1
        - 最新 master（自己部署的）
        - 不确定
    validations:
      required: true
  - type: input
    id: desktop-version
    attributes:
      label: 桌面端版本（可选）
      description: 装的是桌面版安装包才填。抄安装包文件名里的版本号即可；没用过桌面版就留空。
      placeholder: 例如 1.0.1
    validations:
      required: false
  - type: dropdown
    id: webview2
    attributes:
      label: WebView2 运行时（可选）
      description: 桌面版依赖它渲染。Windows 10/11 通常自带，可在「设置 → 应用 → 已安装的应用」里搜 Microsoft Edge WebView2 Runtime 确认；没用过桌面版就选不确定。
      options:
        - 已安装
        - 未安装
        - 不确定 / 没装桌面版
    validations:
      required: false
  - type: textarea
    id: logs
    attributes:
      label: 日志 / 截图
      description: 把截图直接拖进这个框，或贴 data/server.log 的内容（黑窗口里的英文照抄）。这是定位问题最快的信息。
    validations:
      required: true
  - type: dropdown
    id: install-method
    attributes:
      label: 安装方式
      options:
        - 桌面端安装包（Windows .exe / NSIS）
        - Windows 双击 install.bat
        - node install.mjs（原生）
        - node install.mjs（Docker 模式）
        - bash setup.sh
        - docker compose 手动部署
        - 其他 / 还没到安装这一步
    validations:
      required: true
  - type: dropdown
    id: os
    attributes:
      label: 操作系统（可选）
      description: 没有你的选项就选「其他」。
      options:
        - Windows 10
        - Windows 11
        - Ubuntu 24.04
        - macOS
        - 其他
    validations:
      required: false
  - type: input
    id: node-version
    attributes:
      label: Node.js 版本（可选）
      description: 终端执行 node -v 查看；没装过就写"没装"。
      placeholder: 例如 v22.23.2
    validations:
      required: false
  - type: dropdown
    id: verify
    attributes:
      label: 确认一下（必选）
      description: 为了确保反馈真实有效，请选一项。选哪个都不影响处理。
      options:
        - 我实际运行过，上面的内容是真实的
        - AI 辅助写的，但我已按真实运行情况逐条核对
    validations:
      required: true
