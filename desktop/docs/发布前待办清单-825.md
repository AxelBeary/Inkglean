# 拾绘桌面版 · 发布前待办清单（825 建）

> 发布前剩余事项的**唯一汇总处**（各处零散开放项以本文为准，办完一项勾一项）。分组按「没有它就发不了」到「最好别欠」排列；勾不齐第一组，发布免谈。

## 一、硬依赖（缺一项都不能发）

- [ ] **1. 公网后端升 v73**——桌面登录依赖设备账本与桌面登录接口（后端批已提交入库，公网尚未部署，你说过服务器暂不更新）。动作：部署后端批（accept.ps1 全绿 + 公网复验）。这是桌面登录的地基。
- [ ] **2. CF 免验证通道执行**——手册就绪（`docs/comms/CF-免验证通道执行手册-20260824-待后台执行.md`），你在 CF 后台照第三节做。不做的话桌面端登录高概率撞上人机验证页。
- [ ] **3. 更新通道五步**（`docs/comms/更新通道执行手册-20260825-待后台执行.md`）：①本机生成正式密钥对 ②公钥换进 tauri.conf.json（现在的占位公钥签不出能用的包）③GitHub 配 2 个 secret + 1 个 variable ④服务器建 desktop-updates 目录 ⑤tag 首发 + 四件产物投递；外加手册里的三条发布后验收（装机/更新/防投毒抽查）。
- [ ] **4. SignPath 签名 或 回退拍板**——申请（条件已核实满足，表已备好，等你下令提交）+ 批下来后接进 CI 流水线挂钩位；申请失败自动回退「不签名 + 下载页指引」。不签名不指引直接发 = SmartScreen 弹窗吓跑画师。
- [ ] **5. 桌面端功能施工批**（主体仍在途；826 壳层商业化批已完成待终验合入）——✅已完：自绘标题栏+窗口所有权（默认/最小 1200×600 + 几何记忆）、方向 A 长卷首页 + 板块契约档①、显隐/装裱/专注、撕悬浮三件、开机自启、字号 14~20、登录页含本地模式入口、四板块+挂牌云端真数据；**826 壳层商业化批**：托盘常驻（菜单+左键唤隐）、关闭行为双选项（最小化到托盘，Rust 侧全路径拦截）、全局快捷键 Ctrl+Alt+S、系统通知（留言待审首场景）、单实例常驻、Cargo authors 清扫。⬜待做：F1~F8 核心环本地侧（文件归档/本地记账/约稿条/小票/图缓存/档案/墨环自动识别二期）、托盘快照（二期评估：现托盘已常驻，快照推送另立题）、模块开关（档②）。
- [ ] **6. 双端回流三项**——F9 客户快查卡 / F11 可接单·已满标识（网页端欠账）/ F12 完稿引导，拍板口径与桌面施工同批落地。
- [x] **7. 正式图标与产品标识**（826 图标批销账）——朱砂印章「拾」字印全尺寸落地（ico/png 全尺寸/icns/NSIS Square 全套，任务栏/托盘/安装器换装）；源资产在 `desktop/src-tauri/icons/source/`（SVG+生成脚本，改色可重生成）。

## 二、发布前应完成（强烈建议，不勾要写明理由）

- [ ] **8. 管理后台设备清单页**——后端接口就绪、页面未建。没有它画师的桌面设备看不见也踢不掉，安全口径一的「设备清单可单台踢出」闭环缺最后一环。
- [ ] **9. desktop 门禁进 CI**——现在 lint/test/build/cargo check 只本地跑，流水线没有强制。发布前把 desktop job 加进 ci.yml（口径照 824 建齐的门禁体系）。
- [ ] **10. 第三方接入首期项**（Eagle 双向 + 通用 CSV/Excel 导入，824 已拍板）——若进首发：开工前先清两项技术复核（Eagle 版本门槛 / 爱发电接口清单）；若不进首发：在此明确砍出范围，别到发布前才纠结。
- [x] **11. desktop 侧 token 注入层**（825 波0 完成：`desktop/src/styles/paper-ink.css`，值移植自 web 亮色作用域；单一事实源收敛留后续批）
- [ ] **12. 下载/分发入口**——画师从哪拿到 exe（下载页还是直链）要定；SignPath 走回退时下载页还要带指引文案。

## 三、验收欠账（建议发布前清零，别滚雪球）

- [ ] **13. 2FA 批本机验收**——测试画师点「重新绑定」看既有会话立即失效、绑定失效提示出现、重绑后 Passkey 复活；邀请入驻二维码页刷新可恢复、「已扫码但刷新了」找回入口可用。
- [ ] **14. 搬家批真站验收**——工具箱两页：价目分享卡 / 小票打印机的分段切换、挑作品弹窗、导入真实档位、老草稿回显。
- [x] **15. 桌面壳首启冒烟**——825 波0 收尾实测批已销账：`npm run tauri dev` 窗口首启、TOTP 登录端到端走通（用户亲测）、首页 7 板块骨架渲染正确、设备账本记账核实（90 天期准确）。同批根治两处重大 bug：①Docker 镜像构建缺 shared/ 源码（容器重建必断）；②updater 端点 {{ }} 模板语法致 dev 启动 panic（CI 改打包前替换注入）。

## 四、拍板项（发布前定掉）

- [ ] **16. 发布版本号与口径**——现在 0.1.0；首发叫 beta 还是正式版？版本号怎么定？
- [ ] **17. 二期项是否进首发**——爱发电同步 / ICS（默认不进，明确写死即可）。
- [ ] **18. S2 点印款式**——分段控件朱砂点印改印章 ✓ 还是保持圆点（anti-slop 复检残留项，小事，顺手定）。

## 五、明确不在本次发布范围（备案，别惦记）

- 增量更新（备案，重评＝包体显著膨胀）
- macOS / Linux / 手机平板（824 拍板：不做、全备案）
- Ko-fi 实时、Bluesky（预留，主体功能做完再议）
- Passkey 桌面端（二期）、代写系统日历（二期）
- 本地数据上传平台兼容（待拍板，默认不动 810 双模式铁律）

---

> 维护纪律：办完一项勾一项并注明日期/批次；新增发布前事项一律进本文，不再散落各 STATUS 开放项；发布当天按本清单逐项核勾。
