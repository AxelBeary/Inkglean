# 桌面端（拾绘桌面版）子项目状态

> 🔑 **最新：2026-08-26 壳层商业化批（worktree feat/desktop-shell-commercialize，门禁全绿+冒烟过，待用户终验后合入）**——「太简陋不像商业化应用」专项第一仗，REQ-014 首发拍板三件（托盘/系统通知/全局快捷键）一次补齐：①**托盘常驻**：启动即建（setup），右键菜单「显示拾绘/退出拾绘」、左键唤隐切换（抬起时刻判定防误触）、tooltip「拾绘桌面版」，图标随 bundle.icon 单一事实源（正式图标组落地后自动跟随）；②**关闭到托盘**：墨笔菜单「关闭时」双选项激活（直接退出/最小化到托盘），偏好落 localStorage（布局只存本地同款口径）+启动即同步壳层（App.vue，不依赖菜单渲染）；拦截在 Rust on_window_event CloseRequested——自绘关闭按钮/Alt+F4/标题栏右键全路径命中不留死角，托盘菜单「退出」走 app.exit(0) 为唯一真退出通道；③**全局快捷键 Ctrl+Alt+S** 唤隐主窗口（拾绘＝S；Rust 侧注册不经 IPC，口径已登记可改）；④**系统通知**：tauri-plugin-notification + bridge/notification.ts（逃生门变体：非关键路径不抛只静默，权限拒/失败一律降级不打扰），首场景＝留言待审变多即通知（首启取数只记基线不通知防开机打扰）；⑤**单实例常驻**：tauri-plugin-single-instance，二次拉起不开新窗只唤起聚焦既有窗口。毛坯清扫：Cargo authors 「you」→「拾绘」。依赖登记：notification/global-shortcut/single-instance 入 NOTICES + tauri 开 tray-icon feature（编译器明示门控）。**门禁全绿**：lint 0 错 / **31/31**（基线 26+5：通知桥静默降级/关闭偏好读写归一）/ build / cargo check；tauri dev 启动冒烟过（进程存活无 panic，setup 建托盘未抛错）。**用户终验清单**：托盘图标可见/右键菜单/左键唤隐；选「最小化到托盘」后关窗→应用驻留、托盘可唤起；Ctrl+Alt+S 唤隐；二次拉起聚焦既有窗。**下一批队列**：正式图标组（朱砂印章「拾」字印，用户已拍板方向；生成主视觉→tauri icon 全尺寸→托盘/任务栏/安装器换装）→ F1~F8 本地核心环 / 档②模块机制。


> 🔑 **最新：2026-08-26 方向 A 落 desktop/ 真实代码批（用户终验通过，已合入；worktree feat/desktop-home-r2-a）**——壳层：系统标题栏退役 → 自绘天杆 TitleBar（拖窗/最小化/最大化/关闭/重载按钮一转动画），decorations:false 窗口所有权 + window-state 几何记忆 + autostart 自启；撕悬浮三件独立窗口（置顶无框可拖；label 释放延迟坑＝先销毁残留再重建 + 失败回滚 + 拖动权限补发）。首页：方向 A 长卷全还原（题签/卷心/题跋/卷尾），板块契约档①落地（today/ops/msgs/orders 登记 + api@1 预留不校验），拖排退役，定制收敛为显隐+装裱三式+专注收合（布局偏好 γ 只存本地）；双模式 + §4.3 脱机审计整包（本地空态/留言整块隐藏/模式感知）。数据：四板块+挂牌 Bearer 真接口（本地 docker 实测）；终验根治两件大事：①留言/订单分页对象误当裸数组致渲染炸、整页冻结卡「同步中…」→ 纠形 + onErrorCaptured 渲染兜底；②CJK 无 nowrap 保护窄列下一字一行竖叠 → 单行铁律全应用扫描加固。视觉整改（用户逐条点名）：天地杆退役、滚动条纸墨化、按钮全纸签化+全局按钮底子、页底装饰换登录页「远山为幕+倒影」并对齐卷尾线、「数据永不上传」句删除、字号功能 14~20px 整页 zoom（同网页滑块口径）、重载按钮+断网恢复自动重拉。窗口体验拍板：默认 1200×600、**最小 1200×600（用户亲定：不允许放太小，窄窗塌单列仅作保险丝）**、DPI 走 Tauri 逻辑像素。环境坑两件：dev 端口 1420 被 Hyper-V 排除区吞（EACCES）→ 14200 + 本机 CORS 双源登记；测试数据灌入本地 docker 账号（SHI- 订单 8/留言 5/作品 6 图，脚本 workspace/temp/seed-desktop-testdata.ts 幂等）。**门禁全绿**：desktop lint 0 错 / **26/26**（基线 9+17）/ build / cargo check。依赖登记：window-state + autostart 入 NOTICES。**下一批队列**：档②本地模块机制单独立批（开工前拍三件：视图字段清单附件/违规阈值与存储配额数字/沙箱帧载体二选一）；发布前硬依赖见清单。**远端未推**（本地领先多笔，等用户下令）。

> 🔑 **最新：2026-08-25 全天收工批（当批全部落档，明日接手看此条）**——全天队列四段：①A 方向功能安放批（墨笔菜单＝账号区/显隐双处同步/撕悬浮三件/专注/自启/关闭行为/检查更新/网页设置；卷尾＝模式·同步·F8 承诺·关于）；②墨环（圆相）合入经营卡 + 装裱换纸（素纸/界格/花青笺，全局单选防混搭）；③插件化十条两轮拍板（板块契约+本地模块机制；用户亲定口径：布局只存本地、模块先声明联网/读写/联动/后台）；④**规范对抗审计批（用户点名反向 debug）**：红队内攻+六家对照两路子代理，24 项裁决（23 属实/1 部分属实/0 不实），规范升 v0.2：新增实现依托章（跨源隔离/不见 IPC/CSP 物理断网/桥握手口令/心跳保险丝）+ 补 5 字段（entry=panel.js 钉死/minHost/description/settings 壳统一渲染/diagnostics）+ 四态状态机·违规清零·防借道条款·source 改壳判定·示例 JSON 机验通过（原示例实测真缺一逗号，已修）；裁决全记录见规范 §九。事实源：`docs/comms/插件化研判-板块契约-20260825-已拍板.md`（v3.0）+ `docs/comms/拾绘模块规范-v0.1-20260825-已拍板立项.md`（文内版 v0.2）。原型留档：仓库根 `proto-desktop-home-r2-A.html`（功能安放版，含墨环/装裱/菜单），截图 `.impeccable/review/`，复验脚本 `workspace/temp/shot-home-r2.mjs`。**明日接手队列**：用户终验方向 A 原型 → 「A 方向落 desktop/ 真实代码（含壳层：标题栏/状态栏/窗框所有权）」批（档①板块契约随批，拖排退役、显隐+装裱+模块开关为唯三定制）→ 档②模块机制单独立批（开工前拍三件：视图字段清单附件 / 违规阈值与存储配额数字 / 沙箱帧载体二选一）。


> 🔑 **最新：2026-08-25 插件化十条拍板批（架构定案：板块契约 + 本地模块机制）**——用户提议「框架+插件」形态（参照 DeepSeek Harness），研判两轮 + 用户十条拍板全部落定，事实源＝`docs/comms/插件化研判-板块契约-20260825-已拍板.md`（v3.0）+ `docs/comms/拾绘模块规范-v0.1-20260825-已拍板立项.md`。**定案骨架**：①封闭壳 + 第一方板块内建（方向 A 五板块融合口径：今日要办/经营/留言/订单速览/挂牌壳控件）；②模块=本机透明文件（`我的文档\拾绘\modules\`，AI 可直接写），manifest 事先声明联网（外网/内网/无）/读写/联动/后台唤醒，沙箱帧+只读桥+越权单独停用；③服务端 API 变更→模块按 api 版本单独失效/单独更新（不整包发版）；④商店留门不修路。**用户亲定口径两条**：布局偏好（显隐/装裱/已撕）只存本地不同步云端（覆盖 dashboard_prefs 复用口径）；拖排退役，定制收敛为显隐+装裱+模块开关。**双模式口径**：联网职能（挂牌/留言/检查更新/网页设置）本地模式与断网一律隐藏不留禁用态；本地模式空态必做。**下一步**：档①板块契约随「A 方向落 desktop/ 真实代码（含壳层）」批施工；档②模块机制紧随单独立批。前置：用户终验方向 A 原型。


> 🔑 **最新：2026-08-25 首页视觉重做 r2 回稿批（三方向高保真原型已交付，待用户拍板）**——设计需求书接单执行完毕，产出三个真不同世界观方向（均用需求书第六节演示数据）：A「案头长卷」（页面即手卷，两端轴为唯一结构件）/ B「账房桌面」（俯视书案，纸叠厚度为唯一装饰语言）/ C「纸窗一格」（窗格骨架+透光纸面，效率极致）。文件=仓库根 `proto-desktop-home-r2-A/B/C.html`（旧不达标三件已删）。**实机核查全过**：Playwright 双视口实测（1180×780 三方向均恰好一屏零溢出 / 800×600 塌单列不破版 / 挂牌翻转与显隐开关冒烟通过 / 零 JS 报错）；截图留档 `.impeccable/review/`，复验脚本 `workspace/temp/shot-home-r2.mjs`。实测根治两坑：①LXGW WenKai CDN 字体固有行高偏大，加载后撑破一屏（字阶行高已收紧）；②`.stage` 类名与页面容器冲突致行内 span 变 100vh 高（已改名）。**用户拍板（本轮新增）**：应用壳层（标题栏/状态栏/窗框所有权）**不做进原型**，拍板按页面形态看世界观，壳层留待选中方向落 Tauri 真实代码时一并实施。**下一步**：用户已选定方向 A；功能安放批已在 A 原型落地（墨笔菜单＝账号区/显隐双处同步/撕悬浮三件/专注画画模式/开机自启/关闭行为/检查更新/网页版设置入口；卷尾＝模式·同步·F8 承诺·关于；F9~F13 按 824 拍板嵌入式不占首页格子；配置页留网页口径已落为菜单内跳转条目）。待用户验收安放版后，A 方向落 `desktop/` 真实代码（含壳层）。**追加批（用户提议）**：①墨环（圆相）时间统计自用户提供的方向 D 原型合入经营卡（飞白留口 + 一笔圈成动效，数据只存本地承诺随行）；②「换款式」拍板为**装裱换纸**：素纸/界格/花青笺三式、全局单选、按板块混搭不做（防 v3 抢戏复发），入口在墨笔菜单「装裱·纸式」，与显隐并列为唯二定制维度。


> 🔑 **最新：2026-08-25 首页视觉重做筹备批（三版原型全被打回；设计需求书已就绪，待外部设计代理接单）**——波0 骨架视觉连打回三版原型（v1 长长短短 / v2 丑+敷衍 / v3 丑飞了），用户拍板大刀阔斧重做。已完成筹备：①三路调研（开源桌面设计模式 + 信息组织 + 三仓库源码解剖）全部落档；②impeccable 设计总监技能安装完毕（审读无夹带，本会话已识别，PRODUCT.md 产品档案已建）；③环境坑定位：预览窗深色滤镜染黑浅色页，原型必须声明 color-scheme light。**交接事实源 = `docs/comms/桌面首页视觉改造调研-20260825-交接材料.md`；用户拍板外部设计路线：完整设计需求书 = `docs/comms/桌面首页视觉重做-设计需求书-20260825-交外部设计代理.md`（自包含，直接交给设计技能代理出 2~3 方向高保真原型），回稿后进拍板流程。**


> 🔑 **最新：2026-08-25 波0 实测报障修复批（用户复测确认生效）**——用户亲测报三件：拖排无效/板块管理有效/退出登录无效。两处真 bug 均 worktree 修复+门禁全绿+用户复测销账：①**拖排无效根因**：WebView2 原生文件拖放通道（dragDropEnabled 默认开）拦截 HTML5 拖拽事件——关闭该通道 + dragstart 补 setData/effectAllowed 标定；②**退出登录无效根因**：auth 清态后未跳转，用户停留在已失效会话页——登出主动 push 回登录页。**诚实备注**：关文件拖放通道后「直接拖文件进窗口」不再可用（将来 F10 截图录单等若需要，另接原生文件选择方案，届时重评）。**新议题（用户提出）**：首页骨架视觉不满意——已派子代理调研竞品与优秀桌面软件的设计做法，产出后进原型拍板流程。


> 🔑 **最新：2026-08-25 波0 收尾实测批（用户亲测登录走通；同批根治两处重大 bug；远端服务器零触碰）**——新工作纪律生效（用户拍板：专用 worktree/branch 施工 + 测试只走本地 docker + 远端冻结）。**实测链**：本地容器重建（post-merge-deploy -SkipAccept：备份 VERIFY 过/迁移回读 v73/冒烟全过，1 处 WARN 公开画师目录空属预期）→ `npm run tauri dev` 首启 → 用户亲测 QQ+六位码登录走通 → 首页 7 板块骨架渲染正确（板块管理/退出登录就位）→ 服务端证据核实：desktop_devices 记账成功，过期日精确 90 天。**同批根治两处重大 bug（均 worktree 修复+实测验证后合入）**：①Docker 镜像构建从未纳入 `shared/` 源码，`@inkglean/shared` file: 依赖解析失败——自 825 起容器重建必断（Dockerfile 补两步 COPY，独立构建实测全链通过）；②updater 端点配置用 `{{ env_var_or_default }}` 模板语法，该语法仅打包路径生效，dev 运行时解析直接 panic——改静态 `updates.invalid` 占位（IANA 保留域，永不解析），CI 改打包前 pwsh 替换注入真实端点（原模板注入从未生效，发布链同步根治）。**本地环境配套（仅本机、零入库）**：docker-compose.override.yml 临时映射 3000（已登记 .gitignore）+ .env 加 CORS http://localhost:1420。**门禁**：desktop lint 0 错 / 9/9 / build 全绿（worktree 内）。**开放项**：波0 遗留三件（暗色主题移植/布局云端同步/波1 功能施工）；桌面壳进程随窗口关闭退出属预期（再启即回）。


> 🔑 **最新：2026-08-25 桌面功能施工批·波0 地基批（门禁全绿）**——发布前清单 5 号第一波落地：①**登录链**：登录页（首发仅 TOTP，纸墨风）+ auth store + 路由守卫（未登录一律回登录页）；凭证存 **Windows 系统保险箱（DPAPI）**：CryptProtectData 密文绑定当前用户、key 白名单防路径穿越、禁止明文（安全口径一落地）；设备标识入保险箱（账本键，同设备重登改账）。API 接 `/api/auth/desktop/login`（v73）：开发默认连本地服务器（`VITE_API_BASE` 可覆盖），发布构建必须注入公网域名（挂清单 1 号：公网后端升 v73 后定值）。②**token 注入层**：`desktop/src/styles/paper-ink.css`（值移植自 web artist-tokens.css 亮色作用域，出处注释在文件头）——shared 哑组件桌面端可直接用（清单 11 号兑现；单一事实源收敛留后续批评估）。③**首页骨架**：7 板块栅格 + 原生拖排 + 显隐管理 + 本地持久化（布局偏好本地存本机，云端 dashboard_prefs 同步后续波接）；板块数据内容为占位，波1/波2 接入。④更新检查由 App 壳挪入 Home（登录态）。脚手架桥接自检页随路由化退役。**新依赖**：pinia / vue-router@4（坑：vue-router 5 系与 vite 6 peer 冲突，钉 4 系与 web 同款）+ windows crate 0.58（DPAPI，NOTICES 已登记）。**门禁**：lint 0 错 / test 9/9 / build / cargo check 全绿。**开放项**：tauri dev 首启冒烟 + 登录端到端实测（清单 15 号，需本地起 `cd server && npm run dev`）；暗色主题移植；布局云端同步。

> 🔑 **最新：2026-08-25 更新通道施工批（仓库侧全部就绪，服务器侧待用户执行）**——清单下一项兑现（仓库能做的全做完）：①**客户端接线**：tauri-plugin-updater Rust+JS 双侧（JS 侧走 desktop-bridge 逃生门新增 `updater.ts`：checkAndDownloadUpdate/installPendingUpdate）；启动静默检查→后台下载（含验签，验签失败拒装＝防投毒口径②）→询问重启；失败全静默。②**CI 签名流水线** `.github/workflows/desktop-release.yml`：`desktop-v*` tag 触发，windows-latest 构 NSIS → tauri 内置 Ed25519 签名（私钥仅存 CI secrets，永不进服务器＝口径①）→ 生成 latest.json（url 指向自托管）→ Release 产物。③**托管配置**：Caddyfile `/desktop/*` 只读静态块（latest.json 禁缓存）+ compose 只读挂载 `./desktop-updates`；NSIS installMode=currentUser（免管理员）。④**旧版保留**：目录留前 3 版，回滚＝旧 latest.json 覆盖（口径③）。**执行手册**：`docs/comms/更新通道执行手册-20260825-待后台执行.md`（五步：正式密钥对生成/公钥换配置/仓库 secrets+vars/服务器目录/首发投递 + 回滚与验收清单）。**诚实备注**：tauri.conf.json 现 pubkey 为一次性占位密钥对公钥（私钥生成后当场销毁，仅供配置/构建过关），首发前必须按手册换正式密钥对；第三方依赖登记已补（updater + dialog 双件）。门禁：见提交前实跑（desktop lint/test/build + cargo check）。

> ✅ **最新：2026-08-24 anti-AI-flavor 复检批（用户拍板候选 C，零功能改动）**——对照 vibe-hub.org/anti-ai-flavor 标准逐项复检搬家批：纸墨语言本身即 anti-slop 极端风格谱系（文楷/宣纸/手剪纸角/零渐变零玻璃），无硬命中；唯一味点＝分段控件选中态原是组件库默认填色思路，用户拍板改**纸签脉选中态**（纸底 + 花青状态边 + 朱砂点印，同排期卷轴纸签口径，inset 实现零位移状态边）。VL 二轮诚实备注：朱砂点小且形似通知 badge（S2），主辨识靠粗边，点改印章 ✓ 与否待用户裁决（纪律禁三轮）；S3 为验证页既有说明色（人工复验 4.73:1 达标）。measure 绿 / shared 23/23。验证页与截图：`workspace/temp/shared-move-824/`（verify-seg-modal.html / shot-round2.png）。

> 本文件为桌面端子项目专有事实源，与主项目 `docs/comms/STATUS.md` 分工：
> - 平台整体状态/发版/门禁基线 → 主 STATUS
> - 桌面端（`desktop/` 目录）开工进度/专属拍板/验收清单 → 本文件
> 需求决策总书 = `docs/requirements/REQ-014-桌面端伴侣应用.md`（父级文档，所有拍板的唯一事实源；本文件不复制细节，只做索引与运营状态）。

> 🔑 **最新：2026-08-24 第三方接入研判：部分拍板①~⑤全部落地（零代码）**——首期 2 项定案：Eagle **双向**接入（归档写入 + 选图读取）+ 通用 CSV/Excel 文件导入；二期 2 项：爱发电轮询回流（同步订单**视同本地数据可导出**）/ ICS 已拍板照列；**口径拍板：脱网＝不碰公网（Eagle localhost 允许）**，已回写 REQ-014 §模式设计；Ko-fi 实时与 Bluesky 均进预留（主体功能全做完再议）。剩余技术复核 2 项：Eagle 版本门槛 / 爱发电接口清单（施工启动前清掉）。另：用户新提未来议题「本地数据上传兼容（去重/上传/防冲突）」——触碰 810 双模式分离铁律，已派子代理研判，定位重评材料。全文 → `docs/comms/桌面端第三方接入研判清单-20260824-待拍板.md`。

> 🔑 2026-08-24 继续推进两项：接入研判排期落清单 + CF 免验证通道手册就绪（待用户后台执行）——①第三方软件接入研判正式进开工清单（用户指示）：纯研判不动代码，排期钉在「更新通道收尾后、桌面端功能施工批启动前」（研判结论影响 F10 录单导入/素材集成等施工决策，且无前置依赖可随时提前插队）。②清单下一项「CF 规则调整」能自己做的全做完：放行范围已对照代码核实（登录类接口仅 4 条路径，宁窄勿宽）+ 可粘贴的规则表达式 + 质询源诊断步骤 + 验收方法 + 身份标签口径钉死（`X-Shihui-Client: desktop/<版本>`，可伪造、仅分流统计、绝不做放行条件）——全落 `docs/comms/CF-免验证通道执行手册-20260824-待后台执行.md`。**停在此处的原因**：本地无 Cloudflare 凭证，规则需在用户 CF 后台动手（生产安全配置，属拍板/操作停止点）；手册第三节可直接照做。

> ✅ 2026-08-24 共享接线体检全绿（零代码改动，用户要求复核）——现网未碰、测试验真、文档落档三条全兑现：接线实体核实=两端 `file:../shared` junction 链接存活 + 三处冒烟保险丝（shared 骨架 1 例 / web 接线 1 例 / desktop 接线 1 例）在位。**门禁全绿（当次实跑）**：①shared 自身 lint 0 错 / test 1 过 / typecheck 过；②desktop lint 0 错 / test 7 过（含接线保险丝）/ build 过 / cargo check 过；③web 全量 lint（含 typecheck）0 错 / test 118 文件 831 例全过（含接线保险丝）/ check:i18n 无新增违规 / build 成功。**结论**：共享接线完好，既有服务零影响；F3/F4 搬家批仍等用户下令（零搬家纪律不变）。

> ✅ 2026-08-24 后端增量落地——桌面 token 类型 + 设备账本（记账式会话，安全口径一/方案 A，迁移 v73）——开工清单第五步后端侧完成：**新表** `desktop_devices` 设备账本（一张账记全部：登录=记账 / 踢人=撕账 / 顺延=改账 / 设备清单=同账；`(画师, 设备UUID)` 唯一，同设备重登改账不重复记账）。**桌面 token 类型**：会话 payload 增 `client='desktop' + device_id`，过期权威完全落在账本（网页 7 天写死病灶不复制）；**90 天基础期 + 活跃自动顺延**（距过期不足 7 天仍有活跃→改账回 90 天，即每周活跃管到底；活跃记账每小时节流防每请求一写）。**新接口**：`POST /api/auth/desktop/login`（复用 verifyTotpLogin：重放防护/防爆破/锁定/封禁拒绝全套语义与网页一致；下发 Bearer 不下发 cookie；登录留痕 v72 复用）+ 管理端 `GET/DELETE /api/admin/artists/:id/devices`（读账/单台踢出，step-up 守卫自动挂接）。**踢人联动**：单台踢=撕单条账（不动网页会话）；全端踢（bumpTokenVersion/TOTP 重绑重置）=撕光该画师全部账目。**门禁**（全绿）：`cd server` typecheck 三配置全过 / lint 0 错 0 警（eslint+oxlint）/ `npm test` 145 文件 1676 用例全过（基线 +10：新增 `desktop-session.test.ts` 10 例覆盖记账/改账/撕账/过期/顺延/节流/联动/护栏）。**诚实备注**：管理端设备清单仅后端接口就绪，后台展示页未建（后续批）；桌面客户端接线（登录页+凭证存系统保险箱）属桌面端侧待做项。

> ✅ 2026-08-24 共享组件方案定契（用户拍板方案 A：根目录公共仓 + 分两步；零搬家）——仓库根新建 `shared/` 公共仓（包名 `@inkglean/shared`，契约书 `shared/README.md`）：web/desktop 以 `file:../shared` 链接直接导入源码、无构建产物；哑组件纪律（不发请求/不读存储，数据进 props 事件出 emit）；依赖最小（vue 走 peer）；视觉 token 由宿主注入不写死。**接线实证**：两端各加共享包接线冒烟测试（接线断先红的保险丝）。**零搬家纪律**：F3/F4 仍在 web 原地，迁入走单独搬家批（带 web 全量门禁回归），不与功能开发混批。**门禁**：shared 自身 lint/test/typecheck 全绿（首基线 1）；desktop 7/7；web 全量门禁见当次输出。**开放项**：F3/F4 搬家批时机（等用户下令）。

> ✅ 2026-08-24 desktop-bridge 首层落地 + 门禁体系建齐（双侧全绿）——开工清单第三步首层完成：**前端** `desktop/src/bridge/`（env 环境探测 / errors 逃生门错误类型 / index 统一收口），首批能力：ping 健康检查、openWithSystem 调起系统程序、pickDirectory 目录选择；逃生门纪律=纯浏览器环境抛 BridgeUnavailableError，模板 greet 演示页已换成桥接自检页（App.vue）。**Rust 侧** 自定义命令收口于 `src-tauri/src/bridge/mod.rs`（首个命令 bridge_ping 返回版本号；F8 窗口枚举/输入空闲后续仅此处允许）；接入 tauri-plugin-dialog（capabilities 同步放行）。**门禁体系建齐（口径登记）**：`cd desktop` 后 `npm run lint`（eslint，口径对齐 web）/ `npm run test`（vitest+happy-dom，首基线 6/6）/ `npm run build`（vue-tsc+vite）；Rust 侧 `cargo check`；本批四项全绿（lint 0 错 0 警）。工具链新增：eslint10+typescript-eslint+eslint-plugin-vue / vitest4+happy-dom / jiti / @tauri-apps/plugin-dialog。**小坑实录**：①npm install 被沙箱超时截断留残骸致二次安装 ENOTEMPTY，清残后恢复；②eslint TS 配置文件需 jiti 显式安装。**开放项**：SignPath 申请（用户拍板晚点再交，表已备好）；`npm run tauri dev` 桌面壳内首启冒烟（待用户验收）。上一批脚手架条照旧保留。

> ✅ 2026-08-24 Tauri 2 脚手架落地——开工清单第二步完成：官方 create-tauri-app 生成 desktop/ 脚手架（Vue 3 + TS + Vite 6，与 web 同栈），含 tauri-plugin-opener。项目化调整：productName=拾绘桌面版、窗口标题=拾绘、identifier=com.shihui.desktop、包名 artist-commission-desktop、AGPL-3.0 标注；esbuild 安装脚本登记 allowScripts（对齐根目录/web 惯例）；THIRD-PARTY-NOTICES 补「桌面端」登记段。**环境配套（本机首次）**：winget 装 rustup + Rust stable 1.98.0（minimal→默认 profile）；VS Build Tools 2022 已有。**门禁现口径**：`cd desktop && npm run build`（vue-tsc + vite build）全绿；Rust 侧 `cargo check` 全绿（tauri 2.11.5）；eslint/vitest 未引入，功能施工时补齐并更新本条。**事故实录**：脚手架 `--force` 曾吞掉已建成的 desktop/docs/（本文件与 CONTEXT），经 git restore 完整恢复——教训：非空目录跑 create-tauri-app 前先确认 git 干净备份。**开放项**：SignPath 申请（等用户下令）；`npm run tauri dev` 首启窗口冒烟（未跑，属 GUI 交互验证）。

> ✅ 2026-08-24 立项准备（用户拍板两项：同仓开发 + 子项目独立 docs；零代码）——①桌面端于现有仓库建 `desktop/` 目录（与 web/server 平级），不单独开仓库、不单独开开发 profile；②`desktop/docs/` 为桌面端专属状态事实源（STATUS + CONTEXT），自本日起桌面端专属拍板与进度落本文件、不再并入主 STATUS。开工第一步 = SignPath 签名申请（公开仓+AGPL 已达标，等用户下令）；第二步 = Tauri 2 脚手架。开放项：用户研判验收（二轮研判批落档内容）。

## 已拍板决策索引（详请见 REQ-014）

| 领域 | 摘要 | REQ-014 出处 |
|------|------|--------------|
| 框架/平台 | Tauri 2 + Vue 3（与 web 同栈），Windows 首发；desktop-bridge 逃生层强制 | §技术选型 |
| 模式 | 双模式完全分离（本地脱网/云端登录），仅本地数据可导出 | §模式设计 |
| 功能 | F1~F8（一轮）+ F9~F13（二轮）：文件/记账/约稿条/小票/图缓存/档案/时间统计 + 快查卡/录单/排期三视图/完稿宣传链/成品归档再许可 | §功能清单 + §二轮章 |
| 首页 | 7 板块（排期/待办/订单/收入/挂牌/留言/统计卡）复用拖排显隐；配置页留网页；工具箱搬 3 留 16 | §二轮章 |
| 窗口化 | 选 B 可拆悬浮组件（首发仅撕计时器/今日待办/截稿倒计时），专注画画模式首发；布局偏好复用 dashboard_prefs | §二轮章 |
| 体验 | 托盘快照/系统通知/全局快捷键首发；代写系统日历二期；开机自启静默到托盘 | §二轮章 |
| 登录 | TOTP 首发 + 90 天长效会话（方案 A 服务器记账式）+ 设备清单可踢；Passkey 二期 | §二轮章 |
| 更新 | 自托管静默更新（latest.json + NSIS + Ed25519 验签）+ SignPath 代码签名 | §技术选型 |
| 签名 | SignPath（防 SmartScreen 误报）与更新签名（防掉包）两回事都要做 | §二轮章 |

## 安全口径三条（开工前强制基线）

1. **凭证**：记账式会话（设备表一张账：登录记账/踢人撕账/顺延改账/设备清单同账）+ 登录凭证强制存 Windows 系统保险箱（凭据保管柜），不存明文文件
2. **免验证通道**：CF 只放行登录类接口、不整站放行；桌面端「身份标签」落档写死「可伪造、非防线」，真正防线=应用层限流/锁定/重放防护 + AOP
3. **更新防投毒**：签名私钥永不进服务器（CI 签名） / 客户端验签失败拒装 / 服务器保留前几版包可回滚

## 开工任务清单（按序执行）

- [ ] SignPath 开源签名申请（条件已核实满足；失败自动回退「不签名+下载页指引」，无需重拍）
- [x] Tauri 2 脚手架（Vue 3 + TS + 官方插件），`desktop/` 目录结构落地（824，见顶部最新条）
- [x] desktop-bridge 适配层首层（824：前端收口+逃生门+Rust 命令模块；F8 原生命令接入时扩充）
- [x] 共享组件方案定契（824 拍板方案 A：根目录 shared/ 公共仓 + file: 接线，零搬家；F3/F4 迁入另开搬家批）
- [x] 后端增量：桌面 token 类型 + 设备表（记账式会话）；登录留痕复用（824，迁移 v73，见顶部最新条）
- [ ] CF 规则调整：登录类接口免验证放行；身份标签仅分流（824 手册就绪：`docs/comms/CF-免验证通道执行手册-20260824-待后台执行.md`；卡在用户 CF 后台执行，无凭证不代操）
- [~] 更新通道：Caddy 静态托管（只读）+ CI 签名流水线 + 旧版保留（825 仓库侧全部就绪；服务器侧待用户按《更新通道执行手册》执行：正式密钥/仓库配置/目录/首发投递）
- [x] 桌面端门禁体系（824 建齐）：`npm run lint` / `npm run test`（基线 6）/ `npm run build` / `cargo check`
- [ ] 双端回流三项（F9 快查卡 / F11 容量标识 / F12 完稿引导）与桌面端施工同批落地
- [~] 第三方软件接入研判（824 研判建议清单已产出待拍板 → `docs/comms/桌面端第三方接入研判清单-20260824-待拍板.md`；原排期钉在：更新通道收尾后、桌面端功能施工批启动前——研判结论影响 F10 录单导入/素材集成等施工决策；输入=待研判项索引里的五路调研报告）

## 待研判项（未拍板，仅索引）

- **多端调研（手机/平板/macOS/Linux）——✅ 全部拍板完（824 用户）：四端都不做、全备案**。调研落档 → `docs/comms/多端调研-手机平板MacLinux-20260824-已拍板.md`。拍板：①手机/平板都不做先备案（重评＝桌面端稳定运营后看画师真实反馈；若重评倾向 PWA 路线）；②macOS 无限期备案（mac 画师先用网页版；重评＝用户规模变化或免费签名渠道出现）；③Linux 不做备案（重评＝真实需求拉动）。调研事实与路线结论保留备查，不构成任何施工依据。
- **第三方软件接入研判**：五路子代理调研已落档 → `docs/comms/画师软件生态与开放接口调研-20260824-待桌面端研判.md`（824，用户指令：交由桌面端研判是否接入）。要点速览：Eagle 本地 REST API（9.4 分）为素材管理集成首选、Ko-fi/爱发电为订单导入管道、CSP/Procreate/米画师/微信为死墙；Eagle/SD WebUI/ComfyUI/Blender 均为本地服务接口，与桌面端本地优先架构同构。**研判产出（824，部分拍板①~⑤已落地）**：首期 2 项＝Eagle 双向 + 通用文件导入；二期 2 项＝爱发电（同步订单视同本地数据）+ ICS；Ko-fi 实时与 Bluesky 进预留；脱网口径＝不碰公网（localhost 允许）；剩余技术复核 2 项（Eagle 版本 / 爱发电接口）→ `docs/comms/桌面端第三方接入研判清单-20260824-待拍板.md`。
- **本地数据上传兼容研判**（824 用户新提，研判已产出待拍板）：未来是否支持把桌面本地模式记账数据上传平台、如何做去重/上传/防冲突。注意：落地需改 810 拍板的「双模式完全分离、本地与平台订单零关联」铁律，本研判定位重评材料。建议倾向：默认不动铁律；若将来要做，只做单向一次性逐条搬运（复用现有录单 API、服务器零改动、本地原件保留、永不双向）。最大风险＝口径风险 + 三笔翻译损失（本地无 QQ 号/0 元单无入口/收入统计失真）。待用户确认 8 项（根问题：是否动铁律）+ 技术复核 3 项 → `docs/comms/本地数据上传兼容研判-20260824-待拍板.md`。拍板前不构成任何施工依据。


## 接手指引

- **发布前待办唯一汇总**：`desktop/docs/发布前待办清单-825.md`（825 建；硬依赖/应完成/验收欠账/拍板项/不在范围五组；发布前事项一律进该清单，不再散落开放项）
- 接手桌面端工作：先读本文件顶部最新条 + REQ-014「2026-08-24 二轮功能研判拍板」章
- 拍板落档纪律：桌面端专属拍板 → 本文件；跨端/平台级拍板 → 主 `docs/comms/STATUS.md`
- 领地纪律：`desktop/` 与 `web/`、`server/` 并行施工时按文件划领地，共享组件先定契约后动手
- 门禁口径（824 建齐）：`cd desktop && npm run lint && npm run test && npm run build`，Rust 侧另跑 `cargo check`；首启验证=`npm run tauri dev`