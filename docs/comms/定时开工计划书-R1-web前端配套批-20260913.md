# 定时开工计划书 · R1 web 前端配套批

> **性质**：第二轮定时开工分批计划书之一（总调度见 `docs/comms/定时开工总纲-第二轮-时间表与防冲突-20260913.md`）。
> **一句话**：把上一轮 P4 后端批落地的 v75/v76 能力（取证 IP + 内容级下架 + 4 个管理端点）在 web 前端补齐——W1 处置留痕页 / W2 举报来源 IP 列 / W3 下架·恢复双键 / W4 画师下架横幅 / W5 作品已下架态+恢复 / W6 隐私政策补三条 IP 披露。**不做则后端新能力对用户不可见（"记了但看不到"），且 W6 不补就是对外不诚实（诚实测试会红）。**
> **自动化级别**：🟢 **全批全自动施工**（W4/W6 的措辞按工程学/社会学/直觉三维选最优落地，ledger 标注"供审阅、如有异议另批调整"，不停等）。schedule 9/13 **22:01** 触发（web 门禁独占时段）。

---

## 抬头 · 领地与禁区（防冲突第一道防线）

**只许改这些路径（R1 领地）**：
- `web/src/**`，具体涉及：
  - 管理端组件：`views/admin/AdminActions.vue`（**W1 新建**）、`views/admin/ReportManage.vue`（W2/W3）、`views/admin/ArtistManage.vue`（W3）、`views/admin/ArtistDetailDrawer.vue`（W5 恢复键）
  - 画师端组件：`components/ArtistLayout.vue`（W4 横幅）、`views/artist/ArtworkManage.vue`（W5 已下架态）
  - 路由/导航：`router/index.ts`、`components/admin/AdminLayout.vue`
  - API：`api/modules/compliance.ts`（新增 4 方法）、`api/types/{compliance,admin,artist,artwork}.ts`（补字段/类型）
  - 文案：`locales/zh-CN.ts`·`en.ts` 的 `compliance.admin` / `artworks` / `admin` / 画师横幅新区 / `compliance.privacy` 五个区段
  - 测试：`views/admin/__tests__/*`（扩 mock+断言）、`locales/__tests__/honestyCopy.p1-0912.test.ts`（W6 同步日期）

**禁区（碰了就是撞车）**：
- `server/**`（后端 v75/v76 已落地，本轮只接不改）、`desktop/**`、`shared/**`、根 `docs/**`（除本批 ledger）
- `e2e/**`（R3 领地）、`docs/comms/STATUS.md`（只 R4 回写）、`scripts/accept-baseline.json`（R4 同步）
- **`locales` 的 `artistHome.hidden` 区（zh-CN.ts:844 / en.ts:845）**：那是 W7，属 R2 停等批，**R1 一个字不碰**（R1 与 R2 对 locale 是"一写一读且写者不动读者要读的区段"，靠此维持零冲突）
- `api/index.ts`·`api/types.ts` 的 **barrel 结构**（G7 红灯，F-09 刚拆完，新增方法/类型落 `modules/<域>.ts`·`types/<域>.ts`，barrel 自动 re-export，**不回填、不重排 barrel**）
- **资金域**（`order-pricing` / `price*` 等）：W1~W6 与资金无关，一律不碰

**防冲突四条**（复述总纲第 3 节）：① 领地隔离（**locale 单写者 = R1**，本轮第一铁律）；② 时间错开（22:01 web 门禁独占，与 R3 的 E2E build 错开约 2.5h）；③ 台账纪律（写 `ledger_R1.md`，不碰 STATUS）；④ 高风险搬动不做（不碰 G8 归档）。

**事实源**：`docs/comms/ledgers/ledger_P4.md` §9.5（W1~W8 清单）、`docs/comms/落地契约-P4-v75v76-20260913.md`（接口形状/可见性口径）、`docs/comms/研究-P4-IP收集边界与数据服务-20260913.md`（IP 披露范围）。**改法与代码冲突以代码为准**，记进 ledger。

**🔴 前端硬纪律**：
- **中英词条必须同步改**：改 `locales/zh-CN.ts` 必同步改 `en.ts`——`check:i18n` 的 L-14 key-diff（`web/scripts/check-i18n.ts:220-229`）要求中英键集**完全相等**、`vue-tsc` 要求 `en` 可赋值给 `typeof zhCN`，**加一个键只改一边 CI 必红**。新 UI 文案不得硬编码中文（走 `$t`）。
- **不新增 i18n 豁免**：`check:i18n` 存量 13 条豁免**无新增**；本批是加新键（中英成对）+ 改现有键值，正常过门禁。若挪动任何带存量豁免的文件，须迁移旧条目（命中数与总数都断言），不发明新豁免。
- **扩 mock 与断言**：现有 admin 单测是**精确 mock**（只 mock 用到的 complianceApi 方法）——`ArtistManage.ban.test.ts:23-31`、`ReportManage.stepup.test.ts:18-27`、`ArtistDetailDrawer.test.ts:17-34`。W1/W3/W5 加新方法/新按钮时，**这些测试须同步扩 mock 与断言**，否则新按钮无覆盖，违项目诚实测试纪律。
- **行号会漂移**：F-09 拆分战役后 admin 组件已重组，本文所给行号是 9/13 实测快照，施工时**以代码实况为准、搜符号/文字定位**，别钉死行号。

---

## 一、🟢 W1 管理后台「处置留痕」页（全自动）

**后端契约（已就绪，实测形状）**：`GET /api/admin/admin-actions` → `{ rows: AdminActionRow[], total }`（`compliance.routes.ts:105-134`）；`AdminActionRow = {id, admin_id, action, target_type, target_id, reason, admin_ip, created_at}`（`compliance.service.ts:38-48`）；`action` 白名单 9 种（`compliance.routes.ts:33-37`）；查询参数 `?limit&action&targetType&targetId`。

**施工点**：
1. 新建页面组件 `web/src/views/admin/AdminActions.vue`（仿现有 admin 列表页，如 `ReportManage.vue` 的 `el-table` + 筛选形态）：列＝时间/IP/动作/对象(target_type+target_id)/原因；带 `limit` 分页与 `action`/`targetType` 筛选。
2. 路由注册 `web/src/router/index.ts:116-133`（`/admin` children，仿 `:130` 的 reports 行追加一条 `admin-actions`）。
3. 导航项 `web/src/components/admin/AdminLayout.vue:145-159`（`navItems` 注册表，仿 `:149` 追加；分组见 `:140-144`）。
4. API 方法 `web/src/api/modules/compliance.ts`（新增 `getAdminActions(params?)` → `getJson('/admin/admin-actions', {params})`，与 `getReports` 同域；**barrel 不改**）。
5. 类型 `web/src/api/types/compliance.ts`（新增 `AdminActionItem` / `AdminActionsResult`；建议归 compliance 域保持内聚——端点由 compliance 路由产出）。
6. 文案：`compliance.admin` 区加页面标题/列名/筛选标签 + `admin.navGroup`（zh:1977）加导航标签。**中英同步**。

---

## 二、🟢 W2 举报列表加来源 IP 列（全自动）

- **后端**：`getReports` 走 `SELECT *`，行**已带 `report_ip`**（`compliance.service.ts:84-90`、`compliance.test.ts:33`），前端只需展示。
- **施工点**：
  1. `web/src/views/admin/ReportManage.vue:24-38`（列定义区，在 `:38` `created_at` 列后插一列来源 IP）。
  2. 类型 `web/src/api/types/compliance.ts:28-38` `ReportItem` **当前无 `report_ip`**，补上（可选字段，老数据可能为空 → 列展示要容空）。
  3. 文案 `compliance.admin.colReportIp`（中英同步）。
  4. 测试 `web/src/views/admin/__tests__/ReportManage.stepup.test.ts:18-27`（现 mock 了 complianceApi，加列不破坏其断言；**宜补一条 IP 列渲染断言**）。

---

## 三、🟢 W3 画师管理页下架/恢复 + artist_home 举报行双键（全自动）

**后端契约**：`POST /api/admin/artists/:id/home-takedown` → `{success, already?}`、`home-restore` → `{success}`（`compliance.routes.ts:222-257`）；管理列表 `GET /admin/artists` **已返回** `home_takedown_at` + `home_takedown_reason`（`admin-artist.routes.ts:32-39`）；home-takedown **自动继承 step-up 入口闸**（`compliance.routes.ts:27`，已核非猜测）。

**施工点**：
1. `web/src/views/admin/ArtistManage.vue:88-148`（操作列，仿 `:103-116`/`:128-144` 的 ban/unban 两步确认范式，加「下架主页/恢复主页」键）；`:301` 已 import `complianceApi`；`:616`/`:649` 为 ban/unban 调用范式可参照。
2. `web/src/views/admin/ReportManage.vue:65-78`（compact）与 `:104-119`（wide）——`artist_home` 类型举报行当前**只有 ban/unban**，需并列加「下架主页」键（REQ-042 §三 B 的"警告→内容下架→封禁"阶梯中间格）。
3. API 方法 `web/src/api/modules/compliance.ts`（新增 `homeTakedown(id, reason?)` / `homeRestore(id)`，与 `banArtist` 同域同范式）。
4. 类型 `web/src/api/types/admin.ts:11-15` `AdminArtistItem` **当前无 home_takedown_at/reason**，补上；`types/compliance.ts` 加下架/恢复结果类型。
5. 文案 `compliance.admin`（下架主页/恢复/确认弹窗/toast；原因 prompt **复用现有 `compliance.admin.reasonPlaceholder`**，不新造）。**中英同步**。
6. 测试 `ArtistManage.ban.test.ts:23-31`、`ArtistManage.stepup.test.ts`、`ReportManage.stepup.test.ts`（**扩 mock 新方法 + 补下架/恢复按钮的调用与 step-up 断言**）。

---

## 四、🟢 W4 画师后台下架横幅（机制全自动 + 措辞按三维选最优）

**后端契约**：`/api/artist/profile` 返回 `home_takedown: {at, reason} | null`（`artist.routes.ts:135-136`）；`reason`（自由文本处置描述）**仅经此通道回权利人本人**（`security-dto.test.ts:177-199`，DTO 层已纵深收口，不平铺进任意响应体）。

**施工点**：
1. 挂载点 `web/src/components/ArtistLayout.vue:117-127`（`el-main` 内、`<slot/>` 之上，画师登录后全后台可见）；`store.profile` 在此已类型为 `Partial<ArtistProfileResult>`（`:234-237`），`onMounted` 会 `fetchProfile`（`:362-375`）——横幅读 `profile.home_takedown` 渲染，**机制是纯接线**。
2. 数据流已通：`web/src/stores/artist.ts:69-84`（fetchProfile→getProfile）、`api/modules/artist.ts:16`（getProfile）。
3. 类型 `web/src/api/types/artist.ts:98-108` `ArtistProfileResult` **当前无 home_takedown**，补 `home_takedown: { at: string; reason: string | null } | null`。
4. 文案：画师端**新区段**（建议挂 `dashboard` zh:1103 附近或新建 `homeTakedown` 段，**务必避开 `compliance.admin` 区**，否则与本批 W1/W2/W3/W5 撞区——虽同会话串行不会 lost update，但分区清晰利于审阅）。**中英同步**。

**措辞口径（按三维选最优，不停等；ledger 标注供审阅）**：
- 横幅内容＝下架事实 + `reason`（后端已回）+ 整改/申诉指引。
- **申诉渠道据实措辞**：施工时先核实画师端有无公示的联系/申诉渠道（`PlatformManage.vue` 里的 `admin_qq` 是否对画师可见、画师端有无现成"联系平台"文案）——**有则据实引用，无则用中性表述**（如"如有疑问请通过平台公示渠道联系"），**不虚构申诉入口**。
- 社会学依据：通知通道未接通下，横幅是**唯一能送达被下架画师的手段**（STATUS 明确），必须有且要给出路。

---

## 五、🟢 W5 作品「已下架」态 + 管理端恢复键（全自动）

**后端契约**：作品行带 `takedown_at`/`takedown_reason`（`artist-artwork.service.ts:29-30`）；恢复端点 `POST /api/admin/content/artwork/:id/restore` → `{success}`（`compliance.routes.ts:196-214`）**为管理员专属**（画师 token 调不动）。

**关键澄清（scout 疑点 1 已定）**：W5 拆两半——**画师端只读「已下架」态**（画师改不动 admin 恢复端点）+ **管理端恢复键**。不要按字面把恢复按钮放画师端（技术不可行）。

**施工点**：
1. 画师端展示 `web/src/views/artist/ArtworkManage.vue:44-101`(主图卡)/`:104-175`(网格卡)，`:93-97`/`:137-141` 动作区——加「已下架」徽标 + 下架原因悬浮（**只读**）。
2. 管理端恢复 `web/src/views/admin/ArtistDetailDrawer.vue:84-103`（artworks tab，`:323` getArtistArtworks、`:379` deleteArtistArtwork）——加「恢复」按钮（下架不再物理删，可一键恢复）。
3. API 方法 `web/src/api/modules/compliance.ts`（新增 `restoreArtwork(id)` → `postJson('/admin/content/artwork/${id}/restore')`，与 `:20` removeContent 成对）。
4. 类型 `web/src/api/types/artwork.ts:8-19` `Artwork` **当前无 takedown_at/takedown_reason**，补上（画师端 `ArtworkWithTags`、管理端 `getArtistArtworks` 共用此型）。
5. 文案 `artworks` 区（已下架/原因）+ `compliance.admin`（恢复键）。**中英同步**。
6. 测试 `ArtistDetailDrawer.test.ts:17-34,238-282`（精确 mock，**加恢复键需扩 mock + 断言**）。

---

## 六、🟢 W6 隐私政策补三条 IP 披露（诚实义务，方向唯一，全自动）

**为什么必须做**：STATUS 明确"**不做 W6 隐私文书就是对外不诚实**"；研究档 §8.3 已查出 `artists.last_login_ip`（v72）**在隐私政策数据清单里没披露**，而项目有盯着"告知与实际一致"的诚实测试。

**施工点**：
1. `web/src/locales/zh-CN.ts:2264-2313`（`compliance.privacy.sections` 数组，「一、我们收集哪些数据」items 在 `:2268-2277`）——现有 8 条已含 `:2275`「交付文件下载记录（下载时的 IP 与时间）」（download_ip v66），**补三条**：
   - 登录 IP（`artists.last_login_ip` v72，**既存漏披露**，仅管理端可见）
   - 举报提交 IP（`reports.report_ip` v75，用于纠纷取证）
   - 留言提交 IP（v75，**仅管理端可见**，画师与访客界面按 SQL 构造看不见——研究档 §8.3 实证）
   - 措辞**参照现有 items 风格**（如 `:2275` 的"（用途）"写法），如实标注可见范围。
2. `web/src/locales/en.ts:2264+`（同构补三条）+ 中英 `updated`（zh:2262 / en:2267）**移到施工当日日期**。
3. **组件零改动**：`web/src/views/client/LegalDoc.vue:11-19,46-48` 纯数据驱动渲染 `compliance.privacy.sections`，不用改组件。
4. **诚实测试必须同改**：`web/src/locales/__tests__/honestyCopy.p1-0912.test.ts:12-17` **硬断言 `privacy.updated === '2026-09-12'`（中英各一）**——改了隐私正文移日期，就**必须把此测试里的 `2026-09-12` 同步改成新日期**，否则必红。这是"日期跟随正文实改"的**诚实方向**（测试设计意图即如此，见其 `:1-4` 注释），**不是绕过**。`:19-22` 断言 `terms.updated <= privacy.updated`（terms 是 `2026-08-23`，privacy 前移到任何 ≥08-23 的日期都满足，安全）。`:25-88`（notify/setup 段）与 W6 无关，**勿动**。

**措辞口径（按三维选最优，不停等；ledger 标注供审阅）**：披露内容方向唯一（如实告知既有数据收集行为，研究档 R1/R2 已定范围），措辞参照现有隐私政策条目风格即可；属对外文书，落地后**标注供你事后审阅**，如异议另批调整（法律措辞的最终定夺权仍在你）。

---

## 七、本批不做（禁区）

- **W7 主页不可访问文案**（`artistHome.hidden`）：真两难，属 R2 停等批，R1 不碰 locale 的 artistHome 区。
- **W8 E2E compliance 链路**：属 R3（`e2e/**`）。
- `server/**` 任何改动（后端已落地，只接不改）；`api` barrel 重排（G7 红灯）；资金域；`shared/**`·`desktop/**`。
- 主页判定口径统一（红灯 N1，`ledger_P4.md` §9.6 登记，要统一得单独拍）。
- 板块级下架（红灯 N3，选项 C）。

---

## 八、门禁与自检

**跑全套 web 门禁**（🟢 施工后必跑，主代理亲跑不凭子代理汇报）：
- `cd web && npm run lint && npm run test:web && npm run check:i18n && npm run build`
- 基线：web 当前 **876**（124 文件），补 W1 页测试 + W2/W3/W5 扩断言后**取实测数（勿照抄）**；`check:i18n` 存量 **13 条豁免无新增**。
- **完整贴出输出**（测试数、lint 零错误、check:i18n OK、build 成功）；门禁输出不完整 = 交付作废。

**自检关**：
1. `git diff --stat` 确认改动**全落 `web/src/**`**，无越界到 server/desktop/shared/docs/e2e/STATUS。
2. **中英 locale 同步**：`check:i18n` 的 key-diff 绿（中英键集完全相等）；新增键中英成对。
3. **honesty 测试日期已同步**：W6 移了 `privacy.updated` 就改了 `honestyCopy.p1-0912.test.ts` 的断言日期，`test:web` 绿。
4. **扩 mock 反查**：W1/W3/W5 的新方法/新按钮在对应 admin 单测里都有 mock + 断言覆盖（无"加了按钮没测"的诚实缺口）。
5. **locale 分区反查**：R1 改的 locale 区段**不含 `artistHome.hidden`**（那是 R2/W7 的活）——`git diff locales/zh-CN.ts` 确认没碰 `:844` 那条。

**会话内施工顺序建议**（locale 单写者，串行最稳）：先 `api/types`（compliance 共享，串行加类型/方法）→ 各组件（W1 页 / W2 列 / W3 键 / W4 横幅 / W5 态）→ `locale` 各区**顺序改**（compliance.admin → artworks → admin → 画师横幅新区 → privacy）→ 测试（扩 mock + honesty 日期）→ web 门禁。可派 `huiyue-frontend` 子代理分工非 locale 的组件部分，但 **locale 与门禁由主代理串行收口**（凡写 locale 的活不并发）。

---

## 九、ledger_R1.md 写法（台账纪律）

落在 `docs/comms/ledgers/ledger_R1.md`，至少含：
- **改了哪些文件**：W1~W6 逐项列实际文件路径（新建/修改）+ 关键行号（施工后实况）。
- **新增 API 方法与类型**：getAdminActions/homeTakedown/homeRestore/restoreArtwork + 补的 DTO 字段清单。
- **locale 改了哪些区**：compliance.admin/artworks/admin/画师横幅新区/privacy 各加了哪些键（中英成对）；**明确声明未碰 artistHome 区**。
- **W4/W6 措辞**：横幅整改指引实际用的措辞 + 申诉渠道据实核实结论；隐私三条披露的实际文案——**标注"按三维选最优落地，供用户审阅，如异议另批调整"**。
- **W6 诚实测试**：privacy.updated 移到的新日期 + honestyCopy 测试同步改动确认。
- **门禁数字**：web lint / test:web（新基线数）/ check:i18n（豁免无新增）/ build 各结果（完整原文）。
- **扩 mock 覆盖**：哪些 admin 单测扩了 mock+断言。
- **不写 STATUS**：收口由 R4 统一合并回写；本批新增用例数由 R4 同步 `accept-baseline.json`。
