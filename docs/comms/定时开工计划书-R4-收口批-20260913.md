# 定时开工计划书 · R4 收口批

> **性质**：第二轮定时开工分批计划书之一（总调度见 `docs/comms/定时开工总纲-第二轮-时间表与防冲突-20260913.md`）。**本批是 5 份计划书里唯一碰 `STATUS.md` 与 `accept-baseline.json` 的批**，排在所有施工批（R1/R3/R5）之后收口。
> **一句话**：校验 R1/R2/R3/R5 完成 → 合并各 ledger 回写 STATUS → 同步 baseline（web + E2E + 若 R5 改了 server 用例数则含 server）→ 跑全量 17 道门禁 → 生成"等你清单" → 提交待令不 push。
> **自动化级别**：**半自动**（合并/门禁/回写 STATUS 自动，**提交待令不自动 push**）。schedule 9/14 **03:30** 触发（R1/R2/R3/R5 全完成后，全量门禁独占时段；因新增 R5 依赖批占 02:00，本批从原 01:30 顺延到 03:30）。

---

## 抬头 · 领地与禁区（防冲突第一道防线）

**只许改这些路径（R4 领地，均为收口专属）**：
- `docs/comms/STATUS.md`（**一号窗口维护**：合并 R1~R3 成果回写新条目 + 刷看板）
- `scripts/accept-baseline.json`（同步 web + E2E 基线数字 + history 写明链）
- 各 `ledger_R*.md` 的**读取与合并**（不改施工批的原始 ledger，只汇总）

**禁区**：
- 不重跑/不修改 R1~R3 已完成的施工内容（收口只合并不返工；发现问题记进收口记录报你，不自行改施工批领地）
- `web/src/**`（R1）·`e2e/**`（R3）·`server/**`·`desktop/**`·`shared/**`（**本轮 shared 无改动，R4 不碰**，区别于上一轮 P5 的 ink-palette 色值）·`worktrees/**`
- **不做 STATUS 归档搬动**（G8 红灯，高风险文本迁移，须你确认后单独一批；见第二节）

**防冲突四条**（复述总纲第 3 节）：① 领地隔离（R4 独占 STATUS + baseline）；② 时间错开（01:30 独占，前三批已完成）；③ 台账纪律（R4 是**唯一**回写 STATUS 的批）；④ 高风险搬动单列（G8 不在本批）。

**事实源**：`AGENTS.md`「STATUS 体例与归档纪律」+「改动后最小验证清单」+「门禁全绿定义」、各 `ledger_R*.md`、`scripts/accept.ps1`、上一轮 `ledger_P5.md`（收口范式参照）。

**🔴 收口硬纪律**：
- **`accept.ps1` 由收口主代理亲跑，不派子代理**（`accept.ps1:120` 有 `Set-Content` 会写日志目录，子代理按纪律不跑）。
- **门禁输出必须完整贴出**（测试数、lint 零错误）；**门禁输出不完整 = 交付作废**。
- **"门禁全绿"专指 `pwsh scripts/accept.ps1` 十七道全过**；仅跑 CI 四 job 时须写"CI 四 job 全绿"，**不得简称"门禁全绿"**。⚠ 本轮 accept.ps1 的 npx bug 未修（用户已定"先不修、记账待办"），故沿用上一轮**"实质全绿"**口径（见第四节），**不称"accept 十七道全绿"**。
- **不自动 commit/push**：跑绿后**提交待令**，由你明确下令才 commit；push 更需你单独下令。

---

## 一、前置校验（不满足就停下报你，不硬收口）

1. **ledger 齐全**：`ledger_R1.md`（web 施工）、`ledger_R2.md`（W7 施工图）、`ledger_R3.md`（E2E）、`ledger_R5.md`（依赖与 CI 健康）都在。缺哪份 = 哪批没跑或没落 ledger → 停下报你，不替它补做。
2. **前序门禁绿**：R1 的 web 门禁（lint/test:web/check:i18n/build）绿、R3 的 E2E 绿、R5 的 server 门禁（typecheck/lint/test）绿（R2 无门禁，只出施工图）。**任一前序施工批门禁未绿 → 停下报你**，不带病收口。
3. **环境就绪**（跑全量 accept 前探）：全量 accept.ps1 需本地服务/Docker、E2E 端口就绪。**探活用 `curl.exe --noproxy "*"`**（不用 `Invoke-WebRequest`，.NET WPAD 会挂死误判）。环境未就绪则**降级**：记录"环境未就绪，全量 accept 待手动补跑"，先完成 ledger 合并与 STATUS 回写，不带病硬跑。
4. **收口范围界定**（关键）：本批收口 **R1/R3/R5 已完成的 🟢 部分**——R1 的 W1~W6、R3 的 W8、R5 的依赖升级（sharp+vitest）与假绿测试修复。**R2 的 W7（🟡 停等）若你尚未点头，标注"施工图已出待确认、locale 的 artistHome 区未改、未纳入本轮收口"**，不阻塞 R4、不代入门禁。⚠ 若 R5 走了降级路径（vitest 4 breaking 过大、只修假绿测试暂不升级），按 R5 ledger 实况收口，vitest 警报暂留并标注待另批。

---

## 二、合并 ledger → 回写 STATUS（遵守体例，不做归档搬动）

**回写 `docs/comms/STATUS.md`**：
- 加**一条精简的"第二轮定时开工收口"条目**（短标题行 + `- ` 分组要点，汇总 R1 的 W1~W6、R3 的 W8 链路、R5 的依赖升级与 CI 健康、门禁数字、遗留 🟡 项 W7），并**刷新顶部看板**（HEAD 待提交后更新、web + E2E +（若变）server 基线、迁移号仍 v76、下一步）。
- **遵守体例**（AGENTS.md）：单行 ≤200 字；新条目写成"短标题 + 分组要点"，**禁止把整批结论塞进一个长行**（超长行会让 ripgrep 漏报、diff 不可读、精确替换易错）。
- 🔴 **不做归档搬动**：STATUS 正文上一轮 P5 收口时已标注"超体例 5 条（现 7 条）"，本轮 R4 再加一条会更超。按 G8 红灯口径**本批仍不做归档搬动**（文本迁移属高风险，须单独一批 + 字符总量前后差校验），只在收口条里**续标一句**"正文已超体例，待 G8 归档批处理"。
- **桌面端本轮无改动**：不涉 `desktop/docs/STATUS.md`，主 STATUS 收口条不含桌面内容。

---

## 三、同步 baseline（跑 accept 前先做，否则假红）

- **同步 `scripts/accept-baseline.json`**：R1 补了 W1 页测试 + W2/W3/W5 扩断言 → **web 用例数会变**（当前 876）；R3 加了 e11 → **E2E 数会变**（当前 13）；R5 修漏 await **理论不增删 server 用例**（当前 1801），但若 vitest 4 改变计数则以 R5 ledger 实测为准。把 web、E2E（、必要时 server）的数字改为**各 ledger 记录的实测值**，history 写明"第二轮 R1 涨 web、R3 涨 E2E、R5 升 server 依赖（sharp+vitest）"。
- **desktop / shared 本轮不变**（R1~R5 未动 desktop/shared），基线数字不动。
- ⚠ 上一轮 9/12 曾漏同步 baseline 导致下次 accept **假红**，本批务必先同步再跑 accept。

---

## 四、全量门禁 accept.ps1（17 道）+ npx bug 沿用"实质全绿"口径

1. **跑 `pwsh scripts/accept.ps1`**（17 道全量，含 E2E 与防阀）：需本地环境就绪（第一节第 3 条已探）。**完整贴出输出**（各端测试数、lint 零错误、防阀"豁免 0 项"、E2E 数）。
2. **npx bug 处置**（用户已定"先不修、记账待办"，归红灯 N2 门禁工具批）：`test-tamper` 与 `file-size` 两道因 accept.ps1 用 `npm exec --no -- node` 触发 npx 把 node 当缺失包下载而**假红/未跑起**——沿上一轮口径，**用 `node` 直接补跑这两道**（`node scripts/check-test-tamper.mjs` / `node scripts/check-file-size.mjs`），exit 0 即放行。
3. **判读口径**：15 道 accept.ps1 直接绿 + 2 道 node 补跑绿 = **"实质全绿"**，**不称"accept 十七道全绿"**（除非 N2 工具批已修 npx bug，本轮未修）。带 `-SkipE2E` 时须写明"（跳过 E2E）"。

---

## 五、生成"等你清单"（随收口报告交你）

汇总本轮所有需你点头/参与/审阅的项，一份交你（早上看）：
- **🟡 W7 点头**（R2 施工图，推荐方案 A 兼顾文案）——点头后 W7 落地是另批小活。
- **📋 W4/W6 措辞审阅**（R1 已按工程学/社会学/直觉三维落地：画师下架横幅整改指引、隐私三条 IP 披露）——**供你审阅，如异议另批调整**（法律/产品文案最终定夺权在你）。
- **🟡 Y2~Y7**（总纲第 5 节）：S-912 冒烟全单 / 公网解冻（先确认 `admin_qq` 非空）/ 桌面发布硬依赖 / design·r2 原型选定（可派子代理看图评估）/ F12 放行（可派子代理看原型图初评）/ 桌面波 2 拖拽追认。
- **🔴 N1~N4**（总纲第 6 节）：主页判定口径统一 / 门禁工具批（先不修）/ 板块级下架 / 数据服务另立 REQ。
- **🟢 R5 依赖与 CI 健康结果**（已授权自动处理，报告知你）：CI docker flaky 重跑转绿否 / sharp+vitest 已本地升级、PR #7/#8 已关 / glib 警报已 dismiss / **sharp·vitest 的 6 条警报待你 push 后 dependabot 自动关**（这是唯一需你 push 才完成的收尾）；若 R5 走了 vitest 降级路径，则 vitest 升级待另批。
- **🔴 上一轮 G1~G9 + F-31/32/35/36**：指针 `定时开工总纲-时间表与防冲突-20260912.md` 第 6 节，本轮未动。

---

## 六、提交待令（不自动 push）

- 门禁实质全绿后，**汇总本轮改动**（R1 的 `web/src/**` + R3 的 `e2e/tests/e11-*.spec.ts` + R5 的 `server/package*.json`·`server/tests/**`（依赖升级 + 修漏 await）+ R2 的施工图 markdown + R4 的 STATUS/baseline + 各 `ledger_R*.md` + **本轮 6 份编排文档**：总纲`定时开工总纲-第二轮-*` 与 R1~R5 五份计划书，现为 untracked，属本轮产物须一并纳入），生成 commit message 草稿，**提交待令**。
- **git 状态说明**：上一轮 `096c0b77`+`de923472` **仍未 push**；本轮 commit 叠在 `de923472` 之上，push 时机由你定（可本轮 + 上一轮一起 push）。
- **等你明确下令才 `git commit`**；`git push` 更需你单独下令（push 后才会触发 CI 四 job + E2E + CodeQL）。
- 提交前按纪律**核文件归属**：`git status` 逐条确认都是本轮 R1~R4 领地文件，**禁 `git add -A` 误扫并行会话在途代码**（共享工作区纪律）。若发现非本轮的未跟踪文件（如别的会话在途件），不纳入本次提交、记 ledger 报你。

---

## 七、本批不做（禁区）

- G8 STATUS 归档搬动（高风险，单独批）；N2 门禁工具批（accept.ps1 npx bug + check:i18n，用户定先不修）。
- R2 的 W7 落地（🟡 停等你点头，另批）；各黄灯（S-912/公网/发布/原型/F12/波 2）、红灯（N1~N4 + 上一轮 G/F 项）——本批只在"等你清单"里**提醒**，不施工。
- 返工修改 R1/R3 已完成的内容（发现问题报你，不自行改）；`shared/**` 任何改动（本轮无 shared 活）。

---

## 八、收口记录写法（R4 自己的 ledger）

落在 `docs/comms/ledgers/ledger_R4.md`（或直接并入 STATUS 收口条），至少含：
- **前置校验结果**：R1/R2/R3/R5 ledger 齐否、门禁绿否（含 R5 server 门禁）、环境就绪否、本轮收口范围（哪些 🟢 纳入、W7 🟡 待确认未纳入、R5 是否走降级）。
- **合并回写**：STATUS 收口条摘要 + 看板刷新点（web/E2E 新基线、HEAD 待提交）。
- **baseline 同步**：web、E2E 改了哪些数字（取各 ledger 实测值），history 链。
- **全量门禁输出**：accept.ps1 17 道完整结果（各端测试数 / lint / 防阀豁免 0 项 / E2E）+ npx bug 两道 node 补跑结果 + **"实质全绿"判读**。
- **等你清单**：W7 点头 + W4/W6 措辞审阅 + Y2~Y7 + N1~N4 + 上一轮 G/F 指针。
- **提交状态**：commit message 草稿 + git 归属核验 + "待令，未 commit / 未 push（叠在 de923472 之上）"。
