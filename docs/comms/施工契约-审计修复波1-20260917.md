# 拾绘审计修复 · 波1施工契约（2026-09-17）

> 本文件是波1并行施工的**领地划分事实源**。各施工员开工先读本契约 + `docs/comms/审计缺陷修复施工清单-20260917.md`（下称「清单」，每条含真实行号/根因/修复方向/验收）。
> 基线：`master @ 31189c4a`（工作区开工前干净）。主代理调度，收口单跑门禁。

## 一、波1范围（6 路并行，25 条，含全部 3 个 P0）

| 路 | 子代理 | 主题 | 缺陷编号 |
|---|---|---|---|
| SRV-A | huiyue-backend | 认证·会话·邀请·越权 | P0-1, SRV-03, SRV-04, SRV-08, SRV-09, SRV-11, SRV-14, SRV-15 |
| SRV-B | huiyue-backend | app装配·迁移·备份·OG·下载·幂等 | SRV-05, SRV-06, SRV-07, SRV-10, SRV-13 |
| SRV-C | huiyue-backend | 订单统计·风格删除·分期 | SRV-01, SRV-12, SRV-16 |
| WEB-A | huiyue-frontend | 手动录单价格（P0） | P0-2, WEB-01 |
| DSK-A | huiyue-frontend | 模块沙箱帧（P0） | P0-3, DSK-01, DSK-13a, DSK-13c, DSK-13d, DSK-13e |
| SHR-A | huiyue-frontend | 共享价目卡 | SHR-01 |

## 二、领地文件清单（只准改本路文件，越界＝撞车）

**SRV-A**：`server/src/features/invite/invite.service.ts`、`invite.routes.ts`；`features/auth/devices.service.ts`、`auth.routes.ts`、`webauthn.ts`、`auth.service.ts`；`shared/middleware/auth.ts`、`step-up.ts`；`shared/validate.ts`；`features/artist/artist.routes.ts`、`greeting.service.ts`；`features/admin/admin-artist.routes.ts`、`admin-artist-settings.routes.ts`、`admin-system.routes.ts`、`admin-greeting.routes.ts`

**SRV-B**：`server/src/app.ts`（**独占**，OG/迁移/下载/GC 四处接线全归本路）；`features/og/og-meta.service.ts`；`db/migrate.ts`；`index.ts`；`server/scripts/backup-db.ts`；`features/order/order-gallery.service.ts`；`shared/file-sign.ts`；`shared/idempotency.ts`

**SRV-C**：`server/src/features/order/order-stats.service.ts`、`order-create.ts`、`order-status.ts`；`utils/price.ts`；`features/pricing/style.service.ts`、`style.routes.ts`

**WEB-A**：`web/src/components/artist/order/ManualOrderRight.vue`（**独占**）

**DSK-A**：`desktop/src/modules/frame.ts`、`viewData.ts`、`store.ts`、`manifest.ts`、`registry.ts`；`components/home/ModuleFrame.vue`（**独占**）；`bridge/modules.ts`；如需 Rust 侧则 `desktop/src-tauri/src/**`（本路独占 src-tauri）

**SHR-A**：`shared/src/components/PriceCard.vue`（**独占**）

## 三、跨路口径（波1统一约定，不得各改各的）

1. **P0-2 纯前端修**：WEB-A 调整 `ManualOrderRight.vue` 提交序列为「createManualOrder → 先写完全部自定义增项 addExtraItem → 最后 updatePrice(手输价)」，让手输价成为最终覆盖值；同时修 displayPrice 显示口径（WEB-01）。**后端 `order-pricing.ts` 波1不动**（`adjustFinalPrice` 加减法语义在「先增项后改价」顺序下已正确）。
2. **SRV-02（收款指定节点）波1跳过**：需用户拍板「额度池模型是否要改成节点级实收」，属产品语义决策，不在本波。
3. **SRV-17（折扣取整 floor vs round）延波2**：跨 server(`discount.service.ts`) 与 shared(`totals.ts`)，需统一口径 + 回三端 build，单独小批处理。
4. **P0-3 优先前端根本缓解**：DSK-A 首选「禁止非第一方/未签名模块获取 ledger 视图数据」（改 viewData/ModuleFrame/manifest 的视图下发判定 + 来源校验），从数据源头断外泄；Rust 侧 `framenavigated` 导航拦截作为纵深，若超出前端能力则标注「待 Rust 专项」交回收口，不强行改。
5. **SRV-05 只做无争议加固**：给 ogCache 加条目上限（LRU 或超阈值清最旧），不动 5 分钟 TTL 语义。

## 四、硬纪律

1. **领地隔离**：只改第二节列出的本路文件。`app.ts`/`order-pricing.ts`/`ModuleFrame.vue`/`ManualOrderRight.vue`/`PriceCard.vue` 均为单一归属，绝不两路同写。
2. **每条补/扩回归用例**：清单里给了验收断言的（尤其 P0-1/P0-2/SHR-01/资金类），必须落测试；测试文件放各端既有测试目录（`server/tests/*.test.ts`、`web/src/**/*.test.ts`、`desktop/src/**/*.test.ts`、`shared/src/**/*.test.ts`）。
3. **并行期自检口径**（避免看到其他路未完成改动）：只跑 `npm run lint` + **领地相关的**针对性测试（`vitest run <相关测试文件>`）；**全量 typecheck 与全量 test 不在并行期跑**，由主代理收口统一跑。
4. **改 src-tauri 必本地 `cargo check`**（DSK-A 若动 Rust）。
5. **不 git commit / 不 git add**：收口由主代理统一核文件归属后处理（禁 `git add -A`）。
6. **不碰 worktrees/ 目录、不碰其他路文件、不动清单标注「需拍板/跳过/延后」的条目**。

## 五、各路交回 + ledger

- 各写独立 ledger 到 `temp/ledgers/wave1-<路号>.md`（如 `wave1-srvA.md`）：改了哪些文件、每条怎么修的、补了哪些测试、自检结果（lint + 针对性测试输出）、遇到越界/口径疑问。
- 交回主代理格式：每条编号 → 修复摘要（1~2 句）+ 改动文件 + 新增测试 + 自检结论 + 未尽事项。

## 六、收口（主代理单跑，不采信子代理自检）

清点越界写入 → 合并 6 份 ledger → **亲跑四端全量门禁**（server: typecheck+lint+test；web: lint+test:web+check:i18n+build；desktop: lint+test+build+`cargo check`；shared: lint+test+typecheck 并回 web/desktop build）→ 关键路径浏览器冒烟 → 出摘要交用户终验。门禁输出不完整＝交付作废。
