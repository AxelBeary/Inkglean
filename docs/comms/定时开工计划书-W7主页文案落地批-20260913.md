# 定时开工计划书 · W7 主页文案落地批（链式起点）

> **性质**：第二轮**链式推进的起点批**。用户 9/13 已拍板 **W7 = 方案 A**（见 `施工图-R2-W7主页不可访问文案-20260913.md` §三拍板注 + `ledger_R2.md` §八），本批把方案 A 落地进 locale。
> **一句话**：把 `artistHome.hidden`（主页不可访问提示）从"仅给自隐身画师的自助指引"改为方案 A"中性 + 双指引"（同时给自隐身画师「去设置重开」与被下架画师「去后台看原因」两条出路），中英同步。
> **自动化级别**：🟢 **全自动**（口径已拍板，纯落地）。**链式触发 · goal 模式**：本批由主会话手动创建为链条起点（`startAt=now+10min`、`goalEnabled=true`）；goal 模式自持推进到完成后，**链式创建 R3**（见第三节）。

---

## 抬头 · 领地与禁区（防冲突第一道防线）

**只许改这些路径（W7 落地领地）**：
- `web/src/locales/zh-CN.ts` 的 `artistHome.hidden`（`:844`）**这一条键值**
- `web/src/locales/en.ts` 的 `artistHome.hidden`（`:845`）**这一条键值**
- `docs/comms/ledgers/ledger_W7.md`（台账）

**禁区（碰了就是撞车）**：
- **locale 其他区**：`compliance.admin`/`privacy`/`artworks`/`homeTakedown`/`admin` 等 R1 已落地的区**一律不碰**；`artistHome` 段内除 `hidden` 外的其他键也不碰
- `web/src` 其他一切文件（含 `ArtistHome.vue`——纯数据驱动渲染该键，**不用改组件**）
- `server/**`·`desktop/**`·`shared/**`·`e2e/**`·`STATUS.md`·`accept-baseline.json`

**防冲突**：本批是**链式起点**，R1 已 `completed`（locale 其他区已定稿、无在途写者），本批只改 `artistHome.hidden` 一处 → **locale 此刻单写者＝本批，零并发**。这正是把 W7 从 R1 拆出来单独落地的原因（R1 运行时 W7 若并发改 locale 会撞，现 R1 已收工）。

**事实源**：`施工图-R2-W7主页不可访问文案-20260913.md`（方案 A 拍板 + 三维理由）、`ledger_R2.md` §八、`zh-CN.ts:844`、`en.ts:845`、`ArtistHome.vue:6-7`。**最终中英文案字符串以本批实写为准（可在方案 A 口径内微调措辞）**，改法与代码冲突以代码为准。

---

## 一、🟢 施工点（方案 A 落地）

1. **`zh-CN.ts:844`** `artistHome.hidden` 改为方案 A 中文（建议文案，可在口径内微调）：
   > `该主页当前不可访问。若你是店主且主动关闭了展示，可到「设置 → 主页展示」重新开启；若主页是被平台下架，请在你的后台查看原因与指引。`
2. **`en.ts:845`** 同步改英文（建议，用词与站内既有 `Settings → Homepage Showcase` 口径对齐，施工时核 en.ts 现有 artistHome 段用词）：
   > `This page is currently unavailable. If you're the owner and have turned off your showcase, you can re-enable it under Settings → Homepage Showcase. If your homepage was taken down by the platform, check your dashboard for the reason and next steps.`
3. **`ArtistHome.vue` 组件不动**（`v-if status==='hidden'` → `$t('artistHome.hidden')`，纯数据驱动）。
4. **中英必须同步**（`check:i18n` key-diff 要求中英键集相等；本批改的是现有键的**值**、不新增键，键集不变，key-diff 天然绿）。

## 二、门禁与自检

- **跑**：`cd web && npm run check:i18n && npm run build`（纯文案改值，无需全量 `test:web`；但要 `check:i18n` 绿 + `build` 不炸）。
- **自检**：① `git diff` 确认只改了 `zh-CN.ts:844` + `en.ts:845` 两条 `artistHome.hidden`，没碰 locale 其他区、没碰组件；② 中英文案语义对齐（都是"中性 + 双指引"）；③ `honestyCopy.p1-0912.test.ts` 不涉及 `artistHome`（它测 privacy/notify/setup），本批不影响它。

## 三、🔗 链式推进（goal 模式，本批完成后必做）

本批是**链条起点**。goal 模式推进到完成（门禁绿 + `ledger_W7.md` 落盘）后，**必须**执行链式推进：

1. `Get-Date` 获取当前时间。
2. 调 `schedule` 的 `manage_scheduled_task`（`action=create`）创建下一批 **R3**：
   - `title` = `第二轮定时开工 R3 E2E 合规链路批`
   - `schedule` = `{ startAt: {当前时间+10分钟, ISO-8601 带 +08:00}, timezone: "Asia/Shanghai", repeat: { frequency: "none" } }`
   - `payload` = `{ kind: "agentTurn", goalEnabled: true, message: {R3 链式 message} }`
   - **R3 链式 message**（照抄，含它自己的链式指令）：
     > `【绘约·第二轮·R3 E2E 合规链路批·goal 链式】你是绘约主代理，独立 Quest（goal 模式）触发，无其它上下文。读 docs/comms/定时开工计划书-R3-E2E合规链路批-20260913.md + 总纲「链式推进协议」节，按计划书施工（新建 e2e/tests/e11-compliance.spec.ts，API 断言为主、解耦 W7），goal 模式自持推进到 npm run test:e2e 绿 + 写 docs/comms/ledgers/ledger_R3.md。完成后按总纲链式协议创建下一批 R5（依赖与 CI 健康批，startAt=now+10min、goalEnabled=true，message=读 R5 计划书施工+完成后创建 R4）。`
3. `ledger_W7.md` 记一笔"已链式创建 R3 @ {时间}"。

**链条全序**（权威定义见总纲「链式推进协议」节）：**W7 落地 → R3(E2E) → R5(依赖) → R4(收口·链尾)**。R4 是链尾，完成后不再创建，链条结束。

## 四、ledger_W7.md 写法（台账纪律）

落在 `docs/comms/ledgers/ledger_W7.md`，至少含：
- **改了哪两条**：`zh-CN.ts:844` + `en.ts:845` 的 `artistHome.hidden` 改前→改后文案（实录）。
- **门禁**：`check:i18n` + `build` 结果。
- **链式**：已创建 R3 @ {时间}（taskId）。
- **不写 STATUS**：收口由 R4 统一合并回写（W7 从"停等"变"已落地"，R4 的等你清单相应更新）。
