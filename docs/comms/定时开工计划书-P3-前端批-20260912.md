# 定时开工计划书 · P3 前端批

> **性质**：定时开工分批计划书之一（总调度见 `docs/comms/定时开工总纲-时间表与防冲突-20260912.md`）。
> **一句话**：清 web 端几处明确小账（注释数字纠错、管理后台设备补测、图片格式提示补 GIF），并为 F12 网页端完稿引导卡出施工图。
> **自动化级别**：🟢 **三处小修全自动** + 🟡 **F12 出施工图停等确认**。schedule `D+2`（建议 9/14）**09:00** 触发（当天 web 门禁独占时段）。

---

## 抬头 · 领地与禁区（防冲突第一道防线）

**只许改这些路径（P3 领地）**：
- `web/src/**`（含 `web/src/constants/`、`web/src/components/`、`web/src/views/admin/`、`web/src/locales/`、`web/src/__tests__/` 及相关测试）

**禁区（碰了就是撞车）**：
- `desktop/**`（P2，**"卷心 v-show inert" 已核实归 P2 不在本批**）、`server/**`（P4）、根 `docs/**`（P1）
- `shared/**`（P5 独占——**F12 若做成 shared 哑组件会碰此禁区，见第四节跨领地卡点**）
- `docs/comms/STATUS.md`（只 P5 回写）
- `web/src/api/**` 的 barrel 结构（9/12 F-09 刚拆完 27 个域文件 + barrel、156 处下游 import 零改动，本批不重排它，避免动 G7 红灯）

**防冲突四条**（复述总纲第 3 节）：① 领地隔离；② 时间错开（D+2 09:00，web 门禁独占）；③ 台账纪律（写 `ledger_P3.md`，不碰 STATUS）；④ 高风险搬动不做。

**事实源**：修复工单 F-30/F-24、9/12 交接档第 4 节第 7 条（路 3"部分实现"详单在主 STATUS 9/12 条末段）、`docs/comms/桌面端主页重设计落码-施工图-20260904-波1.md`（F12 已拍口径）。**改法与代码冲突以代码为准**，记进 ledger。

**🔴 前端两条硬纪律**：
- **`check:i18n` baseline 按"文件路径 + 违规串原文"记账**（G2 红灯，工具本身未改）：本批改 `locales` 是改现有键的值、不新增键，正常过门禁；但**若挪动任何带存量豁免的文件**（13 条豁免之一），旧豁免会失配被报成新增违规 → 须迁移旧条目（命中数与总数都断言），**不新增豁免、不发明新键**。
- **中英词条同步改**：改 `locales/zh-CN.ts` 必同步改 `en`，否则 L-14 补的 key-diff 门禁会红。

---

## 一、🟢 F-30 注释数字纠错（web 部分，全自动）

| 文件 | 现注释（错） | 改成 | 注意 |
|---|---|---|---|
| `web/src/constants/toolbox.ts`（工单记 `:22`） | "19 个小工具" | **18** | 与下面 ArtistLayout 是"同一件事三个错误数"之二 |
| `web/src/components/ArtistLayout.vue`（工单记 `:366`） | "13 个工具" | **18** | ⚠ 9/12 F-09 拆分战役把 `ArtistLayout` 997→767 拆过，**行号必漂移**，施工时**搜注释文字"13 个工具"定位**，别钉死 `:366`；若该注释已随拆分流到子组件，跟着改到实际所在文件 |

> F-30 的第三处 `shared/src/utils/ink-palette.ts:24`（石绿浅底 `#EAF3EC`→`#E5F1E9`）属 **shared 领地，移交 P5**，本批不碰。第四处 `docs/CONTEXT.md:72` 的"161 DTO"属 **P1 的 F-18**，本批不碰。

---

## 二、🟢 管理后台"设备"整块前端补测（全自动）

- **病灶**（9/12 路 3 登记）：9/3 回流批做的管理后台"设备" tab（`ArtistDetailDrawer` 第 7 个 tab，接 `adminApi.getArtistDevices` / `revokeArtistDevice`）**前端零测试**——265 行的相关测试文件里无一条设备用例，且 **mock 也没登记那两个新方法**。后端已有 10 例兜底，但前端渲染/交互无防线。
- **补测范围**（照设备 tab 实际形态）：
  1. 打开"设备" tab → 列表渲染（`el-table`）；
  2. 踢出设备 → `popconfirm` 确认 → 调 `revokeArtistDevice`；
  3. 三态（加载中 / 空 / 有数据）；
  4. 在途锁（踢出请求在途时防重复点）。
- **mock 补登记**：在对应 `adminApi` mock 里补 `getArtistDevices` / `revokeArtistDevice` 两个方法（照现有 admin mock 口径），否则测试跑起来 mock 缺方法会假失败。
- **施工提示**：先定位设备 tab 组件与那份 265 行测试文件的**实际路径**（9/12 F-09 拆分后 admin 相关组件可能已重组），以代码实况为准，别钉死行数。字段注意：管理端全列裸数组，字段是 `last_login_ip`（与画师端 `login_ip` 不同）。
- **自动化级别**：🟢 补测是明确活（有后端 10 例与组件形态可参照），可自动做；跑 web `test:web` 验证新用例绿。

---

## 三、🟢 F-24 图片格式提示补 GIF（`locales`，从 P1 移交，全自动）

- **背景**：修复工单 F-24 指出"图片格式漏 GIF"，且**界面那句提示比文档更常被看到**——文档部分（`画师使用说明书.md:129,:439`）已在 P1 改，**界面提示句属 web 领地，移交本批**。
- **改法**：`web/src/locales/zh-CN.ts`（工单记 `:1669`，行号会漂移，**搜图片格式提示文案定位**）把图片格式列表补上 GIF；**同步改 en**。
- **甄别**（附录 A 已核实，别改错）：水印 logo **只接受 PNG、刻意拒绝 GIF**（注释写明是设计意图）——那是**水印**入口，与本条的**参考图/交付图**上传入口不是一回事，**不要顺手把水印那句也加 GIF**。
- **自动化级别**：🟢 改现有词条值 + 中英同步，过 key-diff，可自动做。

---

## 四、🟡 F12 网页端完稿引导卡（出施工图停等 + 跨领地协调卡点）

> **已拍口径**（9/4）：网页端先落地 + `delivered` 交付后弹 + 仅图片交付物触发。824 二轮把 F12 定为**双端共享哑组件**（与 F3 约稿条 / F4 小票同属 `shared/`）。

**为什么只能出施工图、不能本批落地**——三重前置卡点：
1. **需原型先行**：9/3 明确"F12 全新交互需先 html 原型"，目前**尚无原型**；原型要你验收后才落码（照 824/9-4 桌面原型的既定流程）。
2. **跨 shared 领地**：F12 做成 shared 哑组件会碰 `shared/**`（P5 独占禁区）。web 侧只是集成宿主壳。→ 组件本体与 web 集成**分属两批**，需协调：要么 shared 组件随 P5 做、web 集成留 P3 第二波；要么 F12 整体单独立批（带 shared 全量门禁 + 回 web/desktop 各跑 build）。
3. **哑组件纪律**：不发请求/不读存储、数据进 props 事件出 emit、视觉 token 由宿主注入不写死（`shared/README.md` 契约）。

**P3 触发时对 F12 只产出施工图**（停等你确认）：
- 交互流程（交付完成 → 判断是否图片交付物 → 弹引导卡 → 发布/跳过）；
- shared 哑组件契约草案（props/emit 形状）+ web 集成宿主壳改哪些文件；
- 原型建议（单文件 html 纸墨风，供你验收）；
- **明确标注跨领地协调方案**（组件归 P5 还是单独立批）。

---

## 五、红灯 G1 若拍板后归本批（本批不主动做）

- **G1 图片链接续期定时器**（=工单 F-02）：`web/src/composables/useSignatureRefresh.ts` 的轮询 10min / 补刷 8min 都晚于真值 5min 过期点。**属行为变更、须先活体实测**（页面停 5~10 分钟看是否先裂图再回）才能定改法（压间隔 / 只改注释加哨兵 / export `FILE_TTL_MS` 做单一事实源）。
- **未拍板前本批一个字不碰**。拍了且实测后，追加进 P3 第二波或单独小批。

---

## 六、本批不做（禁区）

- `web/src/api/**` barrel 重排（G7 红灯，9/12 刻意"宁可少拆也别赌"未动）。
- `shared/**` 任何改动（含 F-30 ink-palette、F12 组件本体）→ P5 或单独立批。
- S-912 冒烟 A 组 18 条走查（黄灯 Y1，需你 TOTP 登录，本批不代替）。

---

## 七、门禁与自检

**小修部分（🟢 一二三节）跑全套 web 门禁**：
- `cd web && npm run lint && npm run test:web && npm run check:i18n && npm run build`
- 基线：web 当前 **869**（124 文件），补设备用例后取实测数（勿照抄）；`check:i18n` 存量 13 条豁免**无新增**。
- **F12 部分**（🟡）本批只出施工图，不跑代码门禁。

**自检三关**：
1. `git diff --stat` 确认改动全落 `web/src/**`，无越界到 shared/desktop/server/docs。
2. F-30 反查：全仓搜"19 个小工具""13 个工具"应已无残留（都改 18）；确认没漏改 F-09 拆分后注释流向的子组件。
3. F-24 反查：水印入口那句"只接受 PNG"**未被误加 GIF**；中英 locales 同步（key-diff 绿）。

---

## 八、ledger_P3.md 写法（台账纪律）

落在本批产物区（建议 `docs/comms/ledgers/ledger_P3.md`），至少含：
- **改了哪些文件**：F-30 两处注释（实际行号）、设备补测文件 + mock、F-24 中英 locales。
- **门禁数字**：web lint / test:web（新基线数）/ check:i18n（豁免无新增）/ build 各结果。
- **F12 施工图**：产出路径 + 跨领地协调方案 + "停等确认"标记（不记成"已完成"）。
- **卡点清单**：F12 三重前置（原型/跨 shared/哑组件）、G1 未拍不碰。
- **自检结果**：三关各一句。
- **不写 STATUS**：收口由 P5 统一合并回写。
