# 定时开工计划书 · P1 文档收口批

> **性质**：定时开工分批计划书之一（总调度见 `docs/comms/定时开工总纲-时间表与防冲突-20260912.md`）。
> **一句话**：把 2026-09-05 文档交叉审计修复工单第 3/4 批里**纯文档、零代码逻辑、每条已给精确改法**的项一次收口，外加 REQ-044 B/C 文案骨架。
> **自动化级别**：🟢 **全自动**（schedule `D+1`（建议 9/13）08:30 触发，逐条按本计划书施工，无需中途确认；卡点项停下标注不硬做）。

---

## 抬头 · 领地与禁区（施工会话开工前必读，防冲突第一道防线）

**只许改这些路径（P1 领地）**：
- 根 `docs/**`（**除** `desktop/docs/**` —— 那是 P2 领地）
- `README.md`、`AGENTS.md`、`changelog.md`、`画师使用说明书.md`、`维护说明书.md`、`OPS.md`、`CONTEXT.md`、`开发→生产切换指南.md`（均在 `docs/` 下）
- `docs/soul/**`、`docs/comms/` 规范文档（模块规范、板块契约等）、`docs/requirements/REQ-044-*`（新建文案）
- `docs/requirements/archive/README.md`、`docs/specs/archive/接口契约清单-v1.md`（仅加 banner / 改死链一行）
- `.github/workflows/ci.yml` **仅 `:77` 注释文字**（F-18 唯一允许的 `.github` 触碰，不动 workflow 逻辑）
- `.gitignore`（F-38 补 `/worktrees/` 一行）
- `design/r2/{A,B,C}.html`、`docs/comms/proto-desktop-home-r2/**`（F-38 仅加"落选勿施工/外部原件"标注横幅，不改原型本体）

**禁区（碰了就是撞车）**：
- `desktop/**`（P2）、`web/**`（P3）、`server/**`（P4）、`shared/**`（P5）
- `docs/comms/STATUS.md`（**只 P5 收口回写**，施工批一律不碰）
- `scripts/accept-baseline.json`、`scripts/server-backup.sh`（P5 或单独批）
- `worktrees/**`（8 月旧代码，一律不碰）
- `.qoder/**`（不在版本库；F-35 岗位定义入库属红灯，未拍不动）

**防冲突四条**（复述总纲第 3 节）：① 领地隔离；② 时间错开（本批 D+1 独占，纯文档无门禁不抢资源）；③ 台账纪律（写 `ledger_P1.md`，不碰 STATUS）；④ 高风险搬动单列（G8 归档搬动、F-38 整目录移 archive **不在本批做**）。

**事实源**：`docs/comms/文档系统交叉审计-修复工单-20260905.md`（F-13~F-40 逐条改法）、`docs/requirements/REQ-044-产品表达与安利能力.md`（A 项已定稿话术）。改法与代码冲突时以代码为准，并记进 ledger。

---

## 一、运维文档修订（F-13~F-18）

| 号 | 文件:行 | 改成什么 | 依据/注意 |
|---|---|---|---|
| **F-13** | `README.md:62`；`OPS.md:90,167,168`；`维护说明书.md:80,246,288,404,546`；`开发→生产切换指南.md:79,136`（共 11 处容器内 curl） | 统一替换为仓库既有写法：`docker compose exec -T web node -e "fetch('http://127.0.0.1:3000/api/health').then(r=>r.text()).then(console.log).catch(()=>process.exit(1))"`；`OPS.md:168` 的 anon-token 那条改 fetch `POST /api/anon-token` | `Dockerfile` 是 `node:22-slim` 无 curl/wget；仓库四处自有写法（compose healthcheck、update.sh、move-to-opt.sh、patrol.sh）全用 node fetch。**另加防再犯句**到 OPS §0 或 §7：`容器镜像是 node:22-slim（无 curl/wget），容器内自检一律用 node -e "fetch(...)"，文档不得写 exec ... curl` |
| **F-14** | `OPS.md:81-91` 整块替换 | 给两条正确恢复路径：① 宿主 node 直跑（先 `docker compose stop web`，再 `node server/scripts/restore-db.ts`，与已验证的 `rollback.ps1:136` 同路）；② 必须容器内时覆盖 ENTRYPOINT：`docker compose run --rm --entrypoint sh web -c "cd /app/server && npx tsx scripts/restore-db.ts"`。**必加判据句**：`只认 RESTORE_OK <路径> 这一行；没打印 RESTORE_OK = 没有恢复，容器有输出、命令返回 0 都不算` | 根因：`entrypoint.sh` 无 `$@`/`shift`，`docker compose run ... npm run restore` 参数被整段丢弃。**Ⓜ 卡点**：`--entrypoint sh` 在 compose V2 + `container_name` 已存在时的行为、uid 1000 写 bind-mount 后 `data/` 属主，须实测（禁在生产库试）——本批只在文档标注"待实测"，不臆断结论 |
| **F-15** | `OPS.md:51` | `DB 最多 3 份` → 三档表 **daily 7 / deploy 2 / weekly 4** | 依据 `backup-db.ts:18-22,36-40`、`daily-backup.bat:3-7`；`CONTEXT.md:76` 已正确，现两份现行文档互相矛盾 |
| **F-15** | `OPS.md:39` 与 `:229`（两处同错） | 改为在**仓库根**执行：`node server/scripts/backup-db.ts`（DB）+ `node server/scripts/backup-uploads.ts`（uploads） | `backup-uploads.ts:27` 用 cwd 相对 `./uploads`，cwd=server 时会备到陈旧的 `server/uploads/` 且静默成功 |
| **F-16** | `维护说明书.md:222-230` | 对 `server-backup.sh` 的推荐改写或标注"产物仅供人工取用，恢复须手工指路" | **文档部分归本批**；⚠ `server-backup.sh` 脚本本体重做（转调 `backup-db.ts --tier daily`）属 `scripts/` 领地 + 行为变更，**移交 P5 评估或单独批**，本批不动脚本 |
| **F-17** | `OPS.md:247-257` | 拆成"形态 A 宿主机 Caddy → `reverse_proxy 127.0.0.1:3000`"与"形态 B 容器 Caddy → `reverse_proxy web:3000`"两块，各给判据，删掉那句跨形态断言 | `Caddyfile:21` 实为 `reverse_proxy web:3000`，现文误称两形态都是 `127.0.0.1:3000`，照抄即全站 502 |
| **F-18** | `CONTEXT.md:74`(v68)、`OPS.md:186`(68 条)、`维护说明书.md:4`(v71)与`:208`(v68)、`README.md:35,78`+`CONTEXT.md:77`(基线数字)、`ci.yml:77`(注释 test 185)、`CONTEXT.md:54`+`README.md:132`+`维护说明书.md:549`(签名 URL 15 分钟) | 迁移号统一 **v74**、迁移条数 **74**；基线数字改**指针**（"以 `server/src/db/migrations/index.ts` 末尾为准""以 STATUS 最新条目为准"）；`ci.yml:77` 注释 `test(185)`→"以 `desktop/docs/STATUS.md` 为准"（**仅注释文字**）；签名 URL **15 分钟→5 分钟** | 🔴 **别改错**：另有 6 处"15 分钟"指 **TOTP 登录锁定**（`auth.service.ts:15-16`），本来就是 15 分钟，**不要顺手改**。**根治建议**：易变数字一律改指针；立文档纪律"**刷新句子时不得删除数字的日期尾巴**"（`CONTEXT.md:72` 的"161 DTO"就是删了日期尾巴升格成现行事实的反面教材） |

---

## 二、变更史补写（F-19）+ 画师使用说明书修订（F-20~F-29）

### F-19 变更史（`changelog.md`）
- 先补两条被引用却不存在的条目：`## v1.0.0 发版记录（2026-08-23）`（`:68` 引它）、`## v1.0.0-beta.3 发版记录（2026-08-18）`（`:145` 引它）——标签真实存在，缺的只是文字。
- 再补 8/24 起 **56 个提交**的零条目。最优先 4 条：① 2FA 绑定完整性加固（缘起公网画师"面包"报障）；② 830 审计批致**旧交付链接一律 403**（客户重按"开始下载"即恢复）；③ 画师自助设备管理与踢出；④ 计价引擎负金额静默损坏根治。
- 桌面端各批**单列"未发布（桌面端在途）"段**，不与网页端混排。
- **口径提醒**：9/3 的"设备清单页"是**管理端抽屉的 tab**；**画师自助**那套在 830 批 H-3 的账号安全页——别混成一条。
- **🔴 卡点（须你先定，见总纲第 8 节）**：深度＝逐批补 56 条（全）vs 只补发版级摘要（约 1/4 工作量，与 changelog"不删历史只标注"纪律不冲突）。**未定之前本批只补两条被引缺条 + 最优先 4 条，其余留占位待你定深度**。

### F-20~F-29 画师使用说明书（`画师使用说明书.md`，按影响排序）

| 号 | 行号 | 改法 | 类型 |
|---|---|---|---|
| F-20 | `:327,:360,:61` | 删"用订单号查单/下载"（8/14 已 410 下线），改为**追踪链接**概念；全文补"追踪链接"说明 | 教坏客户（最优先） |
| F-21 | `:328,:362,:14` | 补一次性下载规则：**60 秒宽限自动锁定 / 3 次半途锁定 + 5 分钟冷却 / 再许可后旧链接必失效** | 误导→白跑 |
| F-22 | `:118-120,:359` | 删"倍率标签页"（v50 已删表） | 白跑 |
| F-23 | `:197-199` | 改稿次数：后端可写 + 客户页渲染，但**画师端从无输入框**。🟡 **卡点**：默认标注"暂未开放"（保守，不凭空补功能）；若要补功能须另立 REQ | 误导（需你定补功能 or 标注） |
| F-24 | `:129,:439` | 图片格式补 GIF；**文档部分归本批**。⚠ 界面那句提示 `web/src/locales/zh-CN.ts:1669` 属 **P3 领地**，移交 P3 | 误导（画师自缚） |
| F-25 | `:45` | 换手机找回："只能找管理员" → 实际账号安全页可**自助重绑** | 白跑 |
| F-26 | 缺章 | **新增**：邀请码入驻四步流程、账号与安全整章（Passkey / 设备清单踢出 / 日历订阅） | 功能被藏起来 |
| F-27 | `:337-348` | 仪表盘"9 个板块"→ **13 块**（10 基础 + 3 可选）；"可显隐四块"→ 自定义首页抽屉；快捷入口"3~9 个"→ **0~18**；收入走势图指针指错页（图在统计导出页） | 误导 |
| F-28 | `:224,:236,:306,:370-375,:116` | 卡片"五个并排按钮"、时间条"四档"（实**五档**）、焦点图"三态"（实**两态**，且与本文 `:12` 自相矛盾）、主题"三底色+五主色"（画师端是**两态按钮**）、增项"三控件"（实**两控件两计价**） | 误导 |
| F-29 | `:4,:25` | 免责句"正文主体为 v0.46 时代"低估陈旧度（最老错误段落 `:327/:129` 出自 **2026-07-25 初版**）；建议"**日期 + 覆盖范围**"拆开写，别"改一行就升日期" | 反向误导 |

> 施工纪律：同一文件内**自下而上改**（先改大行号再改小行号），避免行号漂移。每条改完在 ledger 记"改了哪几行、依据工单第几条"。

---

## 三、治理收口文档（F-34 / F-37 / F-38 可做部分）

### F-34 双轨维护收口（`docs/soul/**` + `AGENTS.md` + `AGENTS-v2.md`）
- 修 12 处失效指针：`soul-02:7,17`／`soul-03:17,18`／`soul-04:22,57` 的 8 个 `.js` 路径（8/19 TS 迁移后全为 `.ts`）；`soul-01:55,57`／`soul-03:29`／`soul-05:46-48` 的 5 个技能名两处住所找不到（**报"未找到"不报"不存在"**，可能住受保护位置）；`soul-04:16` 的 `docs/tasks`、`docs/acceptance` 两个不存在目录；`docs/soul/skills/.../multi-agent-collaboration-setup/` 目录在但无 SKILL.md（空壳登记）。
- `soul-01:3` 决策者占位符 `<user>` 补实；`soul-01:58` container-rebuild 参考归属改指对（内容在 `multi-role-lead-review-workflow/references/`）。
- **收口原则**：可执行事实（路径/命令/门禁/依赖清单/版本）**单一归属** `AGENTS.md` + 两份 STATUS + `.qoder/agents/*.md`；角色文档降为思考框架与判例，出现路径一律写"见某岗位定义文件的领地清单"。
- `AGENTS.md` 与 `AGENTS-v2.md` 都写门禁清单且**双双漏桌面端与共享包**（`AGENTS-v2.md:38` 同缺）→ 命令清单只在 `AGENTS.md` 一处，`AGENTS-v2.md` 改指针。

### F-37 模块机制文档三处回写（`docs/comms/` 模块规范 + 板块契约）
- 示例清单照抄装不上：唯一致拒因是 `minHost:"0.2.0"` > `HOST_VERSION '0.1.0'`。修法＝文档改指**壳内真示例** `desktop/src-tauri/sample-module/mood-weather/manifest.json`（已实测能渲染）+ 改 3 字段（minHost `0.1.0`、views 用已登记项、删 source）。
- 必填件：规范 §五写"身份**五件**"，代码是**六件**（spec/api/minHost/id/name/version）+ `entry` 钉死 `panel.js`（第 7 隐性致拒因）。
- 沙箱载体：文档写 `asset.localhost`、代码用 **data: URL opaque origin**（隔离更强，拍板文档 `:38-39` 自己预留了这一支）→ **回写文档，不改代码**。
- 版本号自指混乱（文件名 v0.1 / 文内 v0.3 / §九 v0.2）→ 标题行钉一句"现行版本"；`板块契约`同病（文件名 v3.0 / L14 v1 / L82 v2.1）。

### F-38 归档与外围（**只做低风险的标注/加行，整目录搬移不做**）
- ✅ 可做：`design/r2/{A,B,C}.html` 加横幅"外部设计原件、缺墨笔菜单/卷尾/墨环/装裱六件功能安放，**勿据此施工**；安放定稿见仓库根同名三件"，B/C 另标"落选勿施工"；`docs/comms/proto-desktop-home-r2/` 同类标注；`.gitignore` 补 `/worktrees/`；`docs/requirements/archive/README.md:27` 死链改一行（指向 `archive-20260819/`）；`docs/specs/archive/接口契约清单-v1.md` 加"已归档"banner（本体仍自称"单一事实源"、快照 12 routes vs 实测 20 文件，加 banner 即可不改数字）。
- 🟡 卡点（须你定，本批只标注不动）：`REQ-030:8/:15` 引用不存在的 `REQ-009` 且逐字引其 L153——三选（从 git 史恢复进 `requirements/archive/` / 改写"原文见 git commit X" / 抄进 REQ-030 自包含）；`820-income-extra-交付报告.md` 归档漏网 + 三份技术债台账（820×2、823）零活引用需你定性。
- 🔴 **不做**：整目录移入 archive（高风险文本搬动，总纲第 3.4 条，须你确认后单独一批 + 字符总量校验）。

---

## 四、REQ-044 B/C 文案骨架（法律两条已拍板，文字可做）

> A 项三层话术已定稿（REQ-044 §二A + 拍板记录），是 B/C 的输入，**禁止拿 Eagle 类比当地毯**（只对懂行者用）。

- **B README 第一屏改造（`README.md`，骨架可做）**：第一屏换为「一句话定位（A 项普通画师口径：把 QQ 接单、Excel 记账、备忘录排期，换成一个网站——客户自助下单查单，你只管画）+ 核心界面截图 + 三行价值点 + 一键安装/在线试用入口」；技术栈/目录结构/测试基线**全部下移**到文档后半部；可做 en 版首屏。🟡 **卡点**：桌面端截图依赖 `design/r2/` 三版原型选定（黄灯 Y4），本批**只留截图占位**；web 端可先取画师仪表盘成品图占位。
- **C 应用详情页文案（新建 `docs/requirements/REQ-044-附件-详情页文案.md`，骨架可做）**：Eagle 式结构＝一句定位 → 三个场景痛点（报价来回打字 / 排期靠脑子 / 交付链接被转发）→ 功能清单 → 核心截图 → 安全与数据主权一段；面向"不会用电脑的画师"：**零术语、零 git 字样、安装步骤 ≤3 步**。🟡 **卡点**：功能清单**最终排序依赖 D 问卷回收**（黄灯），本批只搭结构 + 占位排序；中文先出，英文随 B 的 i18n 后补。
- **E 自查标准**（可顺带写入对外输出流程文档）：「99% 不懂 git 的画师，10 秒内能懂吗？」

---

## 五、本批不做（红灯，虽在 docs 领地但未拍板，禁止施工）

- **F-31** REQ-014 定位（三选一 A/B/C 待你拍）
- **F-32** `开发自参考.md` 处置（数字删成指针 + 归档三大清单节，方式待你拍）
- **F-36** 视觉 token 事实源与"泥金"（实质设计选择待你拍）
- **G4** 830 原报告补归档（若你选 (a) 才动 `docs/comms/`）
- **G6** F-08③「已拍板规则」章节治理选型
- **G8** STATUS 归档搬动

> 这些拍了以后，多数可追加进 P1 的第二次触发，或另立小批。拍板前**本批一个字都不碰它们**。

---

## 六、自检（本批无端门禁，但要过这三关）

1. **Markdown 与链接**：改完的文档标题层级正确、内部相对链接可达（尤其 F-34 修完的 soul 指针、F-38 的 archive 死链）。
2. **零代码逻辑改动自查**：`git diff --stat` 确认改动全落在 P1 领地的 `.md`/`.html`/`.gitignore` + `ci.yml:77` 单行注释；**没有任何 `.ts`/`.vue`/`.rs`/`.json`（除 ci.yml 注释）逻辑改动**。
3. **F-18 反查**：全局搜"15 分钟"，确认改的是签名 URL 那几处、**TOTP 锁定的 6 处一个字没动**。

---

## 七、ledger_P1.md 写法（台账纪律，不碰 STATUS）

落在本批产物区（建议 `docs/comms/ledgers/ledger_P1.md` 或临时区），至少含：
- **改了哪些文件**：逐个列 `文件:行` + 对应工单号（F-xx）。
- **卡点项清单**：F-14 实测待办、F-16 脚本移交 P5、F-19 深度待定、F-23 默认标注、F-24 界面句移交 P3、F-38 定性项、REQ-044 B/C 截图与排序占位——每条写"停在哪、等什么"。
- **自检结果**：三关各一句（链接可达 / 零代码逻辑改动 / F-18 反查 TOTP 未误伤）。
- **不写 STATUS**：收口由 P5 统一合并回写。
