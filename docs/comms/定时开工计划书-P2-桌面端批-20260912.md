# 定时开工计划书 · P2 桌面端批

> **性质**：定时开工分批计划书之一（总调度见 `docs/comms/定时开工总纲-时间表与防冲突-20260912.md`）。
> **一句话**：清桌面端文档债（F-33 纠错 + F-07 发布清单补登），并为大工程"波 2 拖拽改期"出施工图。
> **自动化级别**：🟢 **文档 + 一处无障碍小修全自动**（F-33/F-07/卷心 v-show inert）+ 🟡 **波 2 出施工图停等确认**（大工程、写接口，不无人值守合入）。schedule `D+3`（建议 9/15）**15:00** 触发（与 P4 后端批 09:00 错开 6h，不抢门禁）。

---

## 抬头 · 领地与禁区（防冲突第一道防线）

**只许改这些路径（P2 领地）**：
- `desktop/**` 全部，含：
  - `desktop/docs/CONTEXT.md`、`desktop/docs/STATUS.md`（**仅 F-33 索引表既有错行 + 看板指针纠错，不新增运营条目**）
  - `desktop/docs/发布前待办清单-825.md`（F-07 补登）
  - `desktop/README.md`（F-33 孤儿改写）
  - `desktop/src/**`（波 2 拖拽，🟡 出施工图停等）
  - `desktop/src-tauri/**`（波 2 若涉 Rust，**改后必须本地 `cargo check`**）

**禁区（碰了就是撞车）**：
- 根 `docs/**`、`README.md`、`AGENTS.md`（P1）
- `web/**`（P3）、`server/**`（P4）、`shared/**`（P5）
- `docs/comms/STATUS.md`（主 STATUS，只 P5 回写；本批连 desktop STATUS 也**只改错行不写新条目**，施工进度记 ledger）
- `worktrees/**`（尤其 `worktrees/desktop-home-r2-a`、`worktrees/desktop-shell-commercialize` 是 8 月旧代码，切过去必撞）

**防冲突四条**（复述总纲第 3 节）：① 领地隔离；② 时间错开（D+3 15:00，与 P4 的 server 门禁错开 6h，desktop 门禁独占时段）；③ 台账纪律（写 `ledger_P2.md`，不写 STATUS 新条目）；④ 高风险搬动不做。

**事实源**：`desktop/docs/STATUS.md` 顶部最新条、`docs/requirements/REQ-014-桌面端伴侣应用.md`、`docs/comms/桌面端主页重设计落码-施工图-20260904-波1.md` §七（波 2 口径）、修复工单 F-33/F-07。**改法与代码冲突以代码为准**，记进 ledger。

**🔴 桌面端两条硬纪律**：
- 改 `desktop/src-tauri/` **必须本地跑 `cargo check`**——9/12 唯一一次 CI 红就是只跑 server/web 没跑 cargo check 造成的"假全绿"。
- 改桌面 UI/样式**必跑宿主级技能 `huiyue-layout-audit` 自检循环**（住 `%USERPROFILE%\.agents\skills\`，不在仓库属正常；VL 评审通道不可用，只能 `measure.mjs` + 人工逐项清单）。波 2 若动布局必走此关。

---

## 一、🟢 F-33 桌面端文档纠错（全自动）

### 1. `desktop/docs/CONTEXT.md` 四条硬错（自 8/24 零回写）
| # | 现文（错） | 改成（代码真值） | 核实点 |
|---|---|---|---|
| a | 本地库 `我的文档\拾绘\data.db` | `app_data_dir/local.db` | 二审已实测该文件真实存在 |
| b | 技术栈列 `tauri-plugin-fs` / `tauri-plugin-shell` | **删除**（全仓无此二插件、连权限声明都没有） | `desktop/src-tauri/Cargo.toml` + capabilities |
| c | "系统通知（含免打扰）" | 删"含免打扰"（**免打扰功能不存在**，全仓只命中这一行文档自身） | 全仓搜"免打扰"仅文档命中 |
| d | "自定义 Rust 仅限 F8" | 桥接层实有 **8 个 `.rs`**、`src-tauri` 全量 **11 个** | `desktop/src-tauri/src/bridge/` 实数 |

- **缺席整块补指针**（不复制细节，避免与 `docs/comms/` 规范双份漂移）：档② 模块机制、暗色主题、工具箱六条目、`schedule/` 纯函数域、本地七张表——每条只加"能力存在 + 指向事实源"一行。

### 2. `desktop/docs/STATUS.md` 索引表纠错（🔴 只改既有错行，不写新条目）
- **看板指针行**（现 `:10` 附近）：把"下方索引表 `:110/:111`"更新为实际行号（工单登记 `:110/:111` 已漂移到 `:122/:123`，**施工时以文件实际行号为准**，改内容前先定位真行）。
- **"已拍板决策索引"表两行**（现 `:122/:123` 附近）：
  - "首页 | **7 板块**…复用拖排显隐" → 代码 `desktop/src/panels/contract.ts:7` 实为 **4 个板块**、拖排**已退役**（8/25 亲定：定制收敛为显隐 + 装裱 + 模块开关）。
  - "窗口化 | …布局偏好**复用 dashboard_prefs**" → 已被 8/25 亲定推翻，`desktop/src/stores/prefs.ts:1` 头注才是对的（**布局偏好只存本地**）。⚠ 工单特别提醒：第一轮曾误判"头注写反话"，**照它改会把唯一正确处改错**——以 `prefs.ts:1` 头注为准。
  - 改法：因这是**运营索引表**（非历史原文快照），更新为当前真值，并在 REQ-014 出处列旁标注"8/25 亲定推翻 8/24 二轮口径"，保留可追溯性。

### 3. `desktop/README.md` 孤儿改写
- 现状：仍是 create-tauri-app 脚手架模板原文、**全仓 0 处引用**。
- 改法：改写为 ≤10 行入口（项目一句话 + 门禁命令 + 指向 `desktop/docs/STATUS.md` 与 REQ-014），或删除。**建议改写不删**（保留一个桌面目录入口）。

---

## 二、🟢 F-07 发布前待办清单补登（全自动，只补 9/12 剩余部分）

> **文件**：`desktop/docs/发布前待办清单-825.md`
> **9/5 已做（不重复）**：销旧 ⚠「desktop-release.yml 仍用 dtolnay」（已由 `983df8c1` 移除）、第 1 项 v73→**v74**、新增第 19/20 项登记两处发布流水线缺陷、第 5/6 项台账刷至 9/4 波 1。

**本批只补 9/12 新发现的部分**（F-06 守卫 9/12 才改成"真拦"，故这条更该补）：
- 明确登记：发版前须配 `vars.DESKTOP_UPDATE_BASE` 与 `secrets.TAURI_SIGNING_PRIVATE_KEY[_PASSWORD]`，**实测当前仓库 variables/secrets 各 0 条**。
- 强调：**F-06 端点守卫已修（9/12 落地）会真拦**——vars 未配则发布流水线按设计**红在第 5 步「注入更新端点」**，不再静默产出坏包。这是"按设计红"，不是 bug。
- 关联指针：具体配置步骤见 `docs/comms/更新通道执行手册-20260825-待后台执行.md`（第 3 步仓库 secrets+vars）。

---

## 三、🟢 卷心两面切换 v-show 无障碍补全（`desktop/src/views/Home.vue`）

> 路 3 台账对账 9/12 新登记项。**经 Grep 核实"卷心"是桌面端术语、`web/src` 零命中**（`Home.vue:459` 卷心月历 pane 用 `v-show` 切换），故归 **P2 而非 P3**——路 3 台账把它混列进了前端待办。

- **病灶**：卷心主位"今日要办 ⇄ 排期月历"两个 pane 用 `v-show` 切换（`mv-pane--cal` 及对应 todo pane），**隐藏的那个 pane 没有 `inert`/`aria-hidden`** → 键盘 Tab 会走进看不见的区域、读屏也会念出隐藏内容。
- **同类病已修参照**：桌面端"更多板块与插件"抽屉关闭态**已加 `inert`**（9/4 波 1，desktop STATUS 有记）——本条照**同一口径**补卷心两面，不发明新写法。
- **改法**：给两个 pane 绑定动态 `inert` 与 `aria-hidden`——`prefs.mainView` 不指向哪个 pane，就给哪个 `:inert` + `aria-hidden="true"`，当前显示的那个解除。🔴 **防绑反**：务必确认"显示的 pane 可交互、隐藏的 pane 才被 inert"，绑反会让卷心整块点不动。
- **测试**：`desktop/src/__tests__/home-mainview.test.ts` 已有"cal pane 存在但 v-show 隐藏"的用例基础，**补一条断言**"隐藏 pane 带 inert/aria-hidden、显示 pane 不带"（这条断言是本批的防绑反硬证据）。
- **自动化级别**：🟢 明确小修（照现成抽屉口径加无障碍属性 + 哨兵测试），可自动做；跑 desktop `lint/test/build`（纯属性不动 Rust，`cargo check` 可略）。

---

## 四、🟡 桌面波 2 拖拽改期（出施工图停等确认，不无人值守合入）

> **口径事实源**：`docs/comms/桌面端主页重设计落码-施工图-20260904-波1.md` §七。波 1 已落"独立排期页三视图**只读**"，波 2 补"写"。

**施工要点（照 web `useQueueTimeline.ts` / `QueueBoard.vue` 口径平移）**：
1. **列表拖排**：`PUT /api/artist/queue/reorder`。
2. **时间条横条两步拖**：`PUT …/deadline`、`PUT …/start-date`。
3. **version 乐观锁接力**：每次写带上 version，成功后用返回的新 version 接力。
4. **409 `ORDER_CONFLICT` 处理**：回滚 + 重拉，不静默吞。
5. **软撤销**：拖错可撤销。
6. **在途序号守卫**：防并发请求错序（照 web 序号守卫口径）。
7. **本地模式波 2 仍无写路径**（本地记账无"开工日"概念）——诚实缺席，不造假按钮。

**🟡 为什么停等**：这是大工程（写接口 + 乐观锁 + 撤销 + 在途守卫 + 可能动布局），涉及多处交互决策与桌面 UI 改动。schedule 触发后**只产出施工图**（拆几步、每步改哪些文件、契约形状、布局审计点），**停下等你确认**再动代码。绝不无人值守直接合入。

---

## 五、本批不做（黄灯/红灯，禁区）

- **桌面波 1 活体终验 12 条**（黄灯 Y1/S-912 C2）：需 `npm run tauri dev` 真机走，属你或桌面代理，本批不碰。**且派工前须先改终验清单 D4 后半句判据**（缓冲区头真值"N 笔候补"被当成"在途"）——这条改判据本身在 `docs/comms/` 属 P1 领地或单独处理，不在 P2。
- **桌面发布硬依赖**（黄灯 Y3）：公网升 v74 / CF 规则 / 密钥对 / SignPath——只有你能做，本批只在 F-07 登记现状，不代做。
- **F-31 REQ-014 定位**（红灯）：虽 REQ-014 是桌面端父文档，但定位改判未拍板，本批不碰。
- **档② 模块机制的"帧是否独立进程"实测**（工单 B5）：需 GUI，交桌面代理或你，本批不做。

---

## 六、门禁与自检

**文档部分（F-33/F-07）自检**（纯 `.md` 改动）：
1. `git diff --stat` 确认只动 `desktop/docs/*.md` + `desktop/README.md`，无代码逻辑改动。
2. CONTEXT.md 四条改完反查：全仓搜 `tauri-plugin-fs`/`免打扰` 应只剩"已删除"的痕迹、无残留声明。
3. desktop STATUS 索引表改完，确认 `:10` 指针行号与表内实际行号一致（不再自相矛盾）。

**波 2 部分**（🟡 出施工图，本批不跑代码门禁；确认后才施工，届时跑）：
- `cd desktop && npm run lint && npm run test && npm run build`
- `cd desktop/src-tauri && cargo check`
- 若动布局：`huiyue-layout-audit` 自检循环（measure.mjs + 人工逐项清单）。
- 基线：desktop 当前 **281**，波 2 新增用例后取实测数（勿照抄）。

---

## 七、ledger_P2.md 写法（台账纪律）

落在本批产物区（建议 `docs/comms/ledgers/ledger_P2.md`），至少含：
- **F-33 改了哪几处**：逐条列文件 + 真行号 + 改前改后（尤其 STATUS 索引表两行、CONTEXT 四条）。
- **F-07 补登内容**：vars/secrets 各 0 条 + F-06 守卫真拦说明。
- **卷心 inert 小修**：改了 `Home.vue` 哪两个 pane + 新断言（显示不 inert / 隐藏 inert）跑绿证据。
- **波 2 施工图**：产出路径 + 拆步摘要 + "停等确认"标记（不记成"已完成"）。
- **文档自检结果**：三关各一句。
- **不写 STATUS 新条目**：波 2 施工进度、F-33/F-07/卷心 inert 完成都由 P5 收口统一回写主 STATUS 与 desktop STATUS。
