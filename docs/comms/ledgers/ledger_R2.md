# ledger_R2 · 第二轮定时开工 · W7 主页文案施工图批

> **性质**：R2 施工台账（不写 STATUS，收口由 R4 统一合并回写）。
> **触发实况**：原 schedule 任务 `73884ed1` 于 2026-09-13 **22:15** 触发，**status = failed**（`execution.state=failed`，updatedAt 23:02）。
> **补救**：主会话于 **23:0x 手工补产出**（R2 只写 markdown、不改源码，主会话可安全代产；重挂 schedule 反而可能再撞并发失败）。

---

## 一、failed 根因（诊断，非猜测）

- **与 R1 并发**：R1（22:01 触发）的 `ledger_R1.md` 写入时间是 **22:24:25**，说明 R1 实际运行 ≥23 分钟；R2 在 **22:15**（R1 仍在运行期）触发 → **两个独立 Quest 并发**。
- **旁证**：同一时段 renpy-aistudio 项目的"夜间集群批1"（22:20 触发，另一会话/项目）也 `failed`；而 23:00 触发的麻将项目任务能正常 `running`（此时 R1 已完成）→ **22:15~22:20 这个窗口的并发/资源竞争把后触发的 Quest 挤失败**。
- **结论**：不是 R2 的 message 或计划书有问题（R1 用同结构 message 成功），而是**编排时把 R2 与 R1 只隔 14 分钟、低估了 R1 的实际运行时长**（误以为"R2 只写 markdown、领地不冲突可与 R1 并发"，但**独立 Quest 执行层不宜并发**）。

## 二、本批产物（主会话补产）

| 产物 | 路径 | 状态 |
|---|---|---|
| W7 施工图 | `docs/comms/施工图-R2-W7主页不可访问文案-20260913.md` | ✅ 已补产出（现状核查 + 方案 A/B/C + 强推荐 A + 落地清单） |
| 本台账 | `docs/comms/ledgers/ledger_R2.md` | ✅ 已补产出 |

## 三、W7 现状核查结论（同载荷铁证）

- `zh-CN.ts:844` `artistHome.hidden`＝给自隐身画师的自助指引语气；`ArtistHome.vue:6-7` 纯文案渲染（改文案不动组件）。
- `artist-visibility.service.ts:15-30`：`isArtistHomeHidden = status==='hidden' ∪ home_takedown_at!=null`，注释明写平台下架与自助隐身**同表现同最小载荷、不区分原因**；`types/artist.ts:42-47` `HiddenArtistProfile` 仅 id/name/subdomain/status → **客户端结构上无法区分自隐身与被下架**。
- R1 已实证未碰 `artistHome` 区（`ledger_R1.md` §三 git diff 零命中），W7 现状未被改动。

## 四、呈给用户（停等点头）

- **选项 A（兼顾文案，🔵 强推荐）/ B（纯中性）/ C（不改，不推荐）**，详见施工图 §三。
- **强化点**：R1 本轮**已落地 W4 画师后台下架横幅**，方案 A 的"若被下架请在后台查看原因"现在有了真实承接（访客侧主页提示 → 画师侧后台横幅闭环成立）。
- **停等标记**：W7 未落地、`locale` 的 `artistHome` 区未碰；等你在 A/B/C 点头（推荐 A），点头后落地是 §四的小活（另批或并入下一轮 web 批，locale 现无并发写者可安全改）。

## 五、教训（记给编排，防后续批再撞）

1. **独立 Quest 不宜并发**：即使是"只写 markdown"的轻批，与施工批并发也会因 Quest 执行层资源竞争 failed。批次间隔必须 **> 前一批的实际运行时长**（不是预估）。
2. **R1 实测运行 ≈23 分钟**（22:01→22:24）——后续绘约批次间隔已按 1.5h 拉开（R3 00:30 / R5 02:00 / R4 03:30），绘约内部不会互相并发。
3. **跨项目并发不可控**：本机 schedule 同时段还有 renpy-aistudio 夜间集群（批2~6：23:30/01:00/02:30/04:00/05:30）与麻将项目任务。绘约 R3/R5/R4 若与它们撞在同一时刻，仍有 failed 风险——**这是多项目共享 schedule 执行环境的固有风险，非绘约编排内部可消除**，需 R4 收口时核验各批 ledger 是否齐全，缺则补救（如本批）。

## 六、不写 STATUS

收口由 R4 统一合并回写。R4 前置校验会查 `ledger_R2.md` 是否在——**现已补产出，R4 不会因缺 ledger_R2 而卡**。

## 七、R2 到点重跑复核记录（2026-09-13，本 Quest 独立验证）

本 schedule 任务到点后重跑，**未重写产物**（施工图与 §一~§六 已完整），只对本会话领地外的只读事实逐项复核，全部与实际代码一致：

| 核查项 | 施工图/本台账所记 | 本会话实读代码核对 | 结论 |
|---|---|---|---|
| 现文案 | `zh-CN.ts:844` 自隐身自助指引语气 | 实测＝`该画师暂未开放主页。如你是店主，请到「设置 → 主页展示」开启「小店展示」。` | ✅ 一致 |
| 英文对应 | `en.ts:845` | 实测＝`This artist's page is currently unavailable. If you're the owner, enable \"Shop visibility\"...` | ✅ 一致 |
| 渲染点 | `ArtistHome.vue:6-7` 纯文案渲染 | 实测＝`v-if="artist?.status === 'hidden'"` → `{{ $t('artistHome.hidden') }}` | ✅ 一致 |
| 同载荷铁证 | `artist-visibility.service.ts:27-30` + 注释 `:15-17` | 实测 `:29` `return a.status === 'hidden' \|\| a.home_takedown_at != null`；注释明写不区分原因 | ✅ 一致 |
| 客户端结构 | `types/artist.ts:42-47` 仅 4 字段 | 实测 `HiddenArtistProfile` = id/name/subdomain/status | ✅ 一致 |
| artistHome 区未碰 | R1 零命中、本批未改 | `git status` 显示 `M zh-CN.ts`·`M en.ts`（R1 的 W1~W6 改动），但 `:844` 仍是原状 | ✅ 一致 |

**本会话纪律执行**：只读源码，领地仅追加本节 `ledger_R2.md`，未改任何 `web/src/**`（含 locale）、未新建/改动施工图（已完整无需重写）、无门禁（🟡 停等未动代码）、不 commit 不 push。**W7 维持停等，等你在 A/B/C 点头（推荐 A）。**

## 八、W7 拍板记录（2026-09-13，用户选定方案 A）

- **用户拍板 = 方案 A**（兼顾文案：中性提示 + 双指引，主页不可访问时同时给自隐身画师「去设置重开」与被下架画师「去后台看原因」两条出路；与 R1 已落地的 W4 后台下架横幅闭环成立）。
- **拍板不改本批定性**：R2 仍是只读批。W7 **落地**（改 `zh-CN.ts:844` + `en.ts:845` 中英同步、跑 `cd web && npm run check:i18n && npm run build`）属 **R2 硬禁区**（只读 locale），须由**另批 / 独立 web 落地批**执行。
- **S-912 联动**：R4 收口的「等你清单」可把 W7 从「停等拍板」降级为「已拍板·待落地」（口径 A），提醒后续排一个 web 小批把文案写进去。
