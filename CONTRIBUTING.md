# 贡献指南

感谢你关注拾绘 Inkglean！无论你是来报 bug、提建议还是贡献代码，这份指南都能帮你少走弯路。

## 我发现了问题，怎么报？

请到 [Issues](../../issues) 新建问题，选择「问题反馈」模板，按模板填写。几个要点：

- **写清楚复现步骤**：从哪一步开始、点了什么、期望看到什么、实际看到什么
- **带上环境信息**：操作系统、Node.js 版本（终端执行 `node -v` 查看）、安装方式（双击 install.bat / node install.mjs / Docker）
- **能截图就截图**：黑窗口里的完整报错、网页上的异常画面，都比口述管用
- **安全问题请不要公开发 Issue**：见下方「安全问题」一节

## 我想贡献代码，怎么开始？

1. **先开 Issue 讨论**：大改动（新功能、架构调整）请先开 Issue 说明想法，达成共识再动手，避免白干
2. **Fork 本仓库**，从 `master` 切出自己的分支
3. **小步提交**：一个 PR 只做一件事；提交信息用中文，格式参考仓库历史（如 `fix: 修复xx`、`feat: 新增xx`）
4. **过门禁再提 PR**（命令均为仓库既有脚本）：
   - 后端改动：`cd server && npm run typecheck && npm run lint && npm test`
   - 前端改动：`cd web && npm run lint && npm run test:web && npm run check:i18n && npm run build`
   - 涉及端到端流程：根目录 `npm run test:e2e`
5. **PR 描述里写清**：改了什么、为什么改、自测结果（门禁通过情况）、涉及界面请附截图

## 代码风格

- 全仓 TypeScript（server/web/e2e 含测试/脚本/配置，无 .js 源码）；后端 strict 全开，禁止用 `any` / `@ts-ignore` 糊弄类型检查
- 前端 Vue 3 + Element Plus，遵循既有 eslint 配置
- 注释写"为什么"，不写"是什么"；与周边代码风格保持一致
- 用户可见文案必须走 i18n（中英文语言包同步补键）

## 许可

本项目采用 [AGPL-3.0-only](LICENSE)。提交贡献即表示你同意你的贡献以相同许可发布，并保证你有权授权该贡献（不侵犯第三方权利）。

## 安全问题

发现安全漏洞（如越权、注入、密钥泄露、认证绕过等）**请勿公开发布**。请使用 GitHub 的「私密漏洞报告」功能（仓库页面 → Security → Report a vulnerability）。哪些算安全漏洞、处理流程与响应约定，详见 [SECURITY.md](SECURITY.md)。

## AI 辅助与自动化

欢迎用 AI 辅助写 Issue 和 PR，但**提交前必须人工核对**：

- **Issue**：AI 帮忙润色文字可以，但"我实际运行过"必须是真的——请自己跑一遍，把真实日志、截图、版本选对。纯 AI 生成、没有真实复现信息的 Issue 会被关闭。
- **PR**：AI 生成的代码必须过仓库门禁（见上），并在 PR 描述里如实说明用了 AI。
- **自动化**：请勿用脚本批量刷 Issue / PR，垃圾信息会被直接关闭并拉黑。

## 不接受什么

- 纯 AI 生成、未经人工核对的 Issue / PR
- 绕过类型检查、跳过测试的"修复"
- 未经讨论的大规模重构
- 与产品定位无关的功能堆砌（拾绘服务对象是画师，功能取舍以画师场景为准）
